# 更新日志

---

## [1.9.7] - 2026-09-02 18:10:00

### ✨ 影人宇宙体验精修：满屏无拉动条 + 顶栏常驻校验刷新 + 探索全量档案精准定位

#### 优化背景与痛点
1. **移动端非满屏与多余边距**：移动端弹窗原采用 `max-h-[95vh] rounded-t-2xl`，未能满屏呈现，在移动端界面显得拘谨且上下割裂。
2. **分类 Tab 溢出与原生横向拉动条**：分类栏塞入「全部、智库、进行中、大满贯」4 个 Tab 的同时还塞入了移动端「校验全网」按钮，导致水平超出屏幕宽度，在移动端与 PC 浏览器中产生极为难看灰色的横向滚动条 `< [====] >`。
3. **点击探索档案后落入页面底部**：在影人卡片中点击「探索生涯全量档案」或点击影人姓名跳转至主界面后，视口停留在此前滚动的位置或页面底部，无法直观看到位于影库顶部的「影人全收录专栏」进度卡片。

#### 核心改动与技术实现
1. **移动端原生级满屏显示 (`components/PersonUniverseModal.tsx`)**：
   - 移动端升级为 `w-full h-full rounded-none` 原生沉浸式全屏布局，彻底消除浮动间距与难看的边角割裂感（桌面端维持 `sm:max-w-5xl sm:max-h-[90vh] sm:rounded-2xl` 视窗）。
2. **Tab 栏 4 等分网格满屏平铺 + 彻底绝杀拉动条**：
   - 将「校验全网 / 全量刷新」按钮上移至顶部标题栏右侧，与关闭按钮并排优雅呈现，释放 Tab 栏空间；
   - 将分类切换重构为 `grid grid-cols-4 gap-1 sm:gap-2 w-full` 等宽满屏网格，4 个分类各占 25% 宽度，文字与计数自适应居中，**零溢出、零换行、绝无任何横向拉动条**。
3. **激活影人专栏后精准定位开头位置 (`App.tsx`)**：
   - 为 `PersonCollectionBanner` 容器绑定 `ref={personBannerRef}`，配置 `scroll-mt-20 sm:scroll-mt-24` 锚点避让；
   - 在 `handleSelectPerson` 中增加 `requestAnimationFrame` + `scrollIntoView({ behavior: 'smooth', block: 'start' })`，点击「探索生涯全量档案」关闭弹窗后瞬时直达该影人全收录专栏开头，视觉焦点精准对齐。
4. **自动化测试与生产构建验证**：
   - 全量 23 个测试套件 172 个用例 100% 全部通过；
   - `npm run build` 8.17s 顺利通过，无任何 TypeScript/打包错误。

---

## [1.9.6] - 2026-09-02 17:55:00

### ✨ 移动端全方位赋能「影人宇宙」：底栏专属导航 + 顶栏常驻金标 + 影库快捷探索卡片

#### 优化背景与痛点
- 桌面端具备醒目的「🏆 影人宇宙」主导航入口与生涯全收集成就体系，但移动端因早期设计将该按钮隐藏（`hidden sm:flex`），且底部导航栏仅有「影库 / 统计 / 打卡 / 助手 / 设置」5 个 Tab，导致移动端用户无法直观进入「影人宇宙」全收集与名导演员生涯代表作排行榜，只能深层折叠在三点抽屉菜单内，入口极度隐蔽，功能感知割裂。

#### 核心改动与体验升级
1. **移动端五段式底部导航栏重构 (`components/MobileNavBar.tsx`)**：
   - 将移动端底栏升级为影视追踪专业布局：`影库`、`宇宙` (🏆 影人脉络宇宙)、`打卡` (居中 FAB 凸起)、`统计` (看板)、`设置`；
   - 新增 `activeTab === 'universe'` 态与 `onOpenPersonUniverse` 快捷回调，附带琥珀金专属光晕与呼吸提示点，单手大拇指一键直达影人全收集榜单；
   - `MobileTab` 类型扩展为 `'library' | 'stats' | 'universe'`。
2. **移动端顶部导航栏常驻金标 (`App.tsx`)**：
   - 移动端顶部标题栏增加专属金色徽章入口 `<button ...><Trophy /> 影人宇宙</button>`，无论用户处于何种浏览深度均可秒级唤起；
   - 顶栏与移动端下拉菜单全面补齐「🤖 AI 观影管家」与「影人脉络宇宙」入口，功能层级平铺直达，彻底解决移动端功能遮蔽问题。
3. **影库列表常驻影人宇宙探索卡片 (`App.tsx`)**：
   - 在移动端影库列表顶部（未激活单人专栏时），优雅呈现渐变卡片 `🌌 影人脉络宇宙 · 名导演员代表作基准 · 阅片大满贯榜 [探索 ➔]`；
   - 引导移动端用户从单片浏览自然过渡到导演/演员宇宙的全收集探索。
4. **移动端弹窗体验与安全区优化 (`components/PersonUniverseModal.tsx`)**：
   - 底部列表滚动区增加 `pb-[calc(env(safe-area-inset-bottom)+3.5rem)]` 深度安全区内边距，彻底杜绝移动设备 Home Indicator 与底部工具栏对底层卡片操作的遮挡；
   - 关闭影人宇宙弹窗后，移动端导航平滑回归当前视图状态，交互无缝衔接。
5. **单元测试与全量构建验证**：
   - 升级 `tests/mobileNavBar.test.tsx` 针对 `宇宙` Tab 及 `onOpenPersonUniverse` 的触发验证；
   - 全量 23 个测试套件 172 个用例 100% 通过；`npm run build` 0 报错通过。

---

## [1.9.5] - 2026-09-02 17:45:00

### 🐛 移动端存储冻结根治：双层缓存重构 + 5MB 配额救护 + 彻底解决 8 月 31 日后记录延迟显示

#### 现象与问题根因
- **问题表象**：移动端每次打开时，界面仅展示今年 8 月 31 日含之前的记录；8 月 31 日之后的最新记录需等待数秒（云同步拉取完毕）才会呈现，关闭应用重新打开后再次复现此现象。
- **深层根因**：
  1. **LocalStorage 5MB 配额击穿**：移动端 WebKit/Safari/Chrome 对 LocalStorage 有严格的 5MB 硬配额。8 月 30~31 日期间片库累计超过 400 部，且部分条目包含庞大的 Base64 本地海报图片（单张即可达数万至数十万字符），加上历史版本遗留的废弃 TMDB 缓存键未清理，导致 LocalStorage 在 8 月 31 日触达配额上限。
  2. **落盘静默失败导致快照被锁死**：原有 `useMovies.ts` 的持久化逻辑直接将包含 Base64 海报的完整 `movies` 数组写入 LocalStorage，在 8 月 31 日触发浏览器的 `QuotaExceededError`。由于定时器中缺乏配额异常挽救机制，写入中断，使得本地存储彻底被冻结在 8 月 31 日的旧快照中。
  3. **云同步数据仅存留于内存**：App 启动 1 秒后触发后台静默云下载（`handleDownload(true)`），从 Gist 拉取了 8 月 31 日之后的新增记录并更新 React state，故“等待几秒才显示出来”；但随后触发的本地落盘因同样超额再次失败，导致新数据一旦关闭页面便随内存销毁，下次打开再次回到 8 月 31 日。

#### 核心实现与架构升级
1. **轻量化分离存储 (`utils/storageUtils.ts`)**：
   - 编写 `stripBase64FromMovies`，在保存至 LocalStorage 前自动剔除 `data:image/` 开头的巨量 Base64 字符串，仅保留文本元数据与体积微小的网络图片 URL；
   - 彻底将数据包体积从 4.8MB 压缩至不足 200KB（体积缩减 95% 以上），400+ 电影仅占用极小空间，再无触顶风险。
2. **启动秒级自愈与空间回收 (`cleanupObsoleteStorage`)**：
   - 应用加载瞬间自动扫描 LocalStorage：主动清除历史遗留的 `cinelog_tmdb_filmography_cache_v1/v2/v3` 废弃键；
   - 一键扫描现有 `cinelog_movies_v1`，将残留其中的历史 Base64 海报自动转存至 IndexedDB 并清洗 LocalStorage，瞬间释放 3MB~4.5MB 宝贵空间。
3. **IndexedDB 双层持久化备份 (`utils/posterStorage.ts`)**：
   - 升级 IndexedDB 数据库版本至 `v2`，新增 `movies_cache` 对象存储区；
   - 新增 `saveMoviesToIndexedDB` 与 `getMoviesFromIndexedDB`，构建「LocalStorage 秒开 + IndexedDB 大容量双层持久化」体系。
4. **配额溢出自动救护与重试机制 (`safeSetItem`)**：
   - 在遭遇任何浏览器的 `QuotaExceededError` 时，自动启动 `emergencyFreeStorage` 清理非核心查询缓存并重新落盘，确保核心观影记录 100% 写入成功。
5. **云同步与增删改即时落盘 (`hooks/useMovies.ts`)**：
   - `syncWithCloud` 合并云端新记录后，云端 Base64 异步写入 IndexedDB，同时调用 `safeSaveMoviesToLocalStorage(merged)` 与 `saveMoviesToIndexedDB(merged)` 立即落盘，杜绝防抖延迟与页面关闭导致的数据丢失；
   - `addMovie`、`updateMovie`、`deleteMovie`、`importMovies` 均接入统一的安全落盘通道。
6. **TMDB 缓存防溢出防线 (`services/tmdbService.ts`)**：
   - 为影人宇宙生平缓存增加容量保护与超限裁剪逻辑，杜绝外部 API 缓存反噬核心数据库空间。

#### 自动化测试与验证
- 新增专项测试套件 `tests/storageQuotaAndSync.test.ts`（6 项测试），覆盖 Base64 剥离、历史废弃键清理、老数据自动救护迁出、配额溢出容灾救护重试及双层存储运作；
- 全量 23 个测试套件 172 个测试 100% 全部通过；
- `npm run build` 生产构建通过。

---

## [1.9.4] - 2026-09-01 20:43:00

### 🚀 卡片详情重构：独立放大展开聚焦视窗 + 彻底杜绝网格整行拉伸

#### 需求背景与问题根因
- **原有痛点**：用户点击卡片右下角「详情」时，卡片内联高度膨胀（从 ~400px 撑大至 900px+）。由于 CSS Grid 默认 `align-items: stretch`，**整行中的所有其他卡片被动同步拉伸，导致同行的其他卡片底部产生大片尴尬空白灰区**；且卡片被限制在 1/4 窄列宽中，长文本剧情简介与追剧流水体验局促、阅读吃力。
- **目标**：实现卡片「单独展开」，并且能「放大该卡片」，使用户更清晰舒适地查看内容，完全不影响同列/同行其他卡片布局。

#### 核心实现与架构升级
1. **独立放大聚焦视窗 (`components/MovieCard.tsx`)**：
   - 将原先内嵌在网格卡片内部的内联折叠区块，升级为通过 `createPortal` 挂载到 `document.body` 的**独立高保真放大视窗**（`max-w-2xl`，宽屏大字体、电影级毛玻璃暗黑背景 `backdrop-blur-md`）。
   - **内容完整放大呈现**：
     - **超大电影级海报头图**：更宽广画幅、高清海报、类型/平台徽章、直接可用的操作快捷键（Markdown 笔记复制、海报生成、编辑、删除）以及右上角关闭按钮 `✕`；
     - **状态与评分阵列**：完整展示追剧状态、良作/神作徽章、多刷火焰徽章、播放倍速（含实际折算观影时长）、TMDB 权威评分与交互星级；
     - **经典台词 Callout**：加粗高亮金句展示；
     - **追剧打卡快捷组件**：`-1` 与 `+1` 快速打卡功能在放大视窗内 100% 交互可用，支持实时查看最近打卡时间；
     - **完整结构化数据**：导演与主演点击可穿透检索专栏、时长、平台、类型、全量标签 Chip；
     - **大版面剧情简介**：长文本无截断、舒适行高、选中文本支持；
     - **影评 / 追剧随笔**：一键复制随笔按钮，分层展示多段深度长评；
     - **分段倍速流水时间轴**：宽屏双列网格结构清晰对比各集区间与倍速；
     - **按天聚合追剧足迹**：多天打卡足迹完整列出；
     - **重温足迹多刷时间轴**：完整展示各刷次心得与轨迹；
     - **底部工具栏**：展示记录日期，右侧提供醒目的「收起卡片 ⌃」按钮。
2. **轻量交互与快捷键支持**：
   - 支持键盘 `Escape` 键一秒收起弹窗；
   - 点击半透明毛玻璃背景或右上角 `✕` / 底部「收起卡片」均可立即平滑收起；
   - 展开视窗时自动锁定底层滚动条 (`document.body.style.overflow = 'hidden'`)，收起时自动恢复。
3. **网格防拉伸加固 (`App.tsx`)**：
   - 为主影库卡片网格与海报墙网格容器添加 `items-start`（`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 sm:gap-6 items-start`），彻底杜绝 CSS Grid 垂直方向强行拉伸同行卡片的问题，同行其他卡片始终保持自然紧凑高度。
4. **正面卡片始终保持纯粹精练**：
   - 列表正面卡片保留封面、标题、徽章、追剧进度条、短评摘要、标签与「详情 ˅」按钮，点击详情平滑聚焦放大，收起后无缝衔接。

#### 自动化测试与验证
- 新增专项测试套件 `tests/movieCardExpand.test.tsx`（3 项测试），覆盖默认状态、点击详情弹出独立放大视窗、收起按钮及 Escape 快捷键逻辑；
- 全量 22 个测试套件 166 个测试 100% 全部通过；
- `vite build` 生产构建成功。

---

## [1.9.3] - 2026-09-01 16:31:00

### ✨ 影人宇宙：生涯基准只统计「导演 / 演员」身份作品

#### 需求
影人宇宙此前会把影人的**访谈、综艺、脱口秀、客串、存档镜头**，以及**编剧、制片、主创等幕后身份**一并计入生涯作品总数，导致收集率被稀释、代表作清单混入无关条目。现统一收紧为**仅统计该影人作为导演或演员的作品**。

#### 实现 (`services/tmdbService.ts`)
1. **演员侧过滤**：新增 `isNonActingCredit()`，命中以下任一条件即剔除：
   - 角色名为本人出镜特征：`self` / `himself` / `herself` / `themselves` / `archive footage` / `cameo` / `uncredited` / `interviewee` / `interview` / `guest` / `host` / `presenter` / `judge` / `contestant` / `panelist`；
   - 节目类型命中非演出类 genre：`10763 新闻` / `10764 真人秀` / `10767 脱口秀`。
2. **导演侧过滤**：`crewCredits` 由原先放行 `Director / Directing / Writer / Screenplay / Story / Producer / Executive Producer / Creator` 收紧为**仅 `Director` 与 `Co-Director`**，编剧、制片、监制、主创一律剔除。
3. **角色合并简化**：去重合并逻辑原先需处理 `编剧` 分支，现只剩 `导演` 与 `主演` 两种身份，合并为 `导演 / 主演`。
4. **缓存键升级**：`cinelog_tmdb_filmography_cache_v3` → **v4**，自动作废浏览器内残留的旧基准（含综艺、编剧条目），**用户重新校验全网即可生效**，无需手工清缓存。

> 备注：`utils/personCatalog.ts` 名导智库为人工维护，经核对所有条目角色均含「导演 / 主演 / 演员」，无需额外过滤。

#### 验证结果
- 新增 3 条回归测试（本人出镜/综艺/客串剔除、仅保留导演职务、导演+主演合并为「导演 / 主演」）；
- 修复 `tests/quickEpisodeUpdate.test.ts` 时间模拟锁定，全量测试 **21 套件 163 测试 100% 全部通过**；
- `src/` 生产代码 **0 个 TS 错误**；`vite build` 生产打包正常通过；
- **生产环境已成功部署上线至 Cloudflare Pages** (`https://movie-tracker-6if.pages.dev`，节点哈希: `cb57b3fb`)。

---

## [1.9.2] - 2026-09-01 16:10:23

### 🐛 Hotfix: 影人宇宙「校验全网」100% 失败 + 反馈失真 + 字段错配

#### 根本原因
- **`services/tmdbService.ts` 使用了 `normalizeTitle` 却遗漏 import** → 运行时在第 647 行抛出 `ReferenceError: normalizeTitle is not defined`；
- 该调用位于 `try {}` **之外**，异常直接冒泡至调用方 `catch`，被统一归类为「同步失败，请检查网络」，**把代码缺陷伪装成网络故障**；
- `build` 脚本为 `vite build`（esbuild 只擦类型、不做类型检查），无人运行 `tsc --noEmit`，缺陷直达运行时；
- 复现率 **100%**：单人「校验全网生平 / 刷新」与「一键校验全网」全部失败，0 条数据入库。

#### 修复内容
1. **补齐缺失导入** (`services/tmdbService.ts`)：新增 `import { normalizeTitle } from '../utils/titleNormalizer';`，彻底修复校验全链路失效。

2. **失败原因可区分** (`services/tmdbService.ts`)：
   - 新增 `PersonFilmographyError` 与 `FilmographyFailureReason`（`not_found` / `auth` / `rate_limit` / `network` / `server` / `unknown`）；
   - `fetchPersonFilmography` 由「一律静默 `return null`」改为：**HTTP 401/403 → `auth`、429 → `rate_limit`、其他异常状态码 → `server`、fetch reject → `network`**，均以 typed error 抛出；仅「TMDB 未收录该影人」返回 `null`；
   - 新增 `describeFilmographyFailure(err, personName)` 与 `getFilmographyFailureHint(reason)`，将异常翻译为可执行的中文提示。

3. **批量校验反馈不再掩盖失败** (`components/PersonUniverseModal.tsx`)：
   - 按 **成功 / 失败 / TMDB 未收录** 三类分别计数后汇总播报，例如 `⚠️ 全网校验未完成（共 15 人）：成功 0 人，失败 15 人。主要问题：TMDB API Key 无效或未配置`；
   - 「主要问题」取占比最高的失败原因（排除「未收录」这一正常结果）；
   - **修复取消提示被覆盖**：原先点「取消」先弹「已取消批量校验」，随即被循环末尾的「已完成」覆盖；现改为独立分支 `已取消批量校验，本次已完成 N/M 人（成功 X 人）`。

4. **误导文案下架** (`components/PersonUniverseModal.tsx` & `components/PersonCollectionBanner.tsx`)：单人校验与专栏 Banner 刷新均接入 `describeFilmographyFailure`，**不再出现「请检查网络」**，代码缺陷类异常直接暴露真实 `err.message` 便于定位。

5. **字段错配修复**：
   - `utils/personCatalog.ts`：本地作品海报由 `m.posterUrl` 更正为 `m.posterImage`（前者恒为 `undefined`，导致代表作清单海报始终不显示）；
   - `App.tsx`：`handleQuickAddPlanning` 的 `posterUrl` 更正为 `posterImage`，快速加入想看时海报不再被静默丢弃；
   - `types.ts`：为 `Movie` 补充 `originalTitle?`（Markdown 归档导入导出实际在用的字段），消除类型与运行时的不一致；
   - `App.tsx`：补齐上述流程缺失的 `Movie` 必填字段 `review` 与 `posterColor`（此前被 TS2353 掩盖，会产出残缺条目导致渲染异常与海报占位渐变失效）。

#### 验证结果
- **生产代码类型全清**：`src/`（`App.tsx`、`components/`、`services/`、`utils/`、`hooks/`）**0 个 TS 错误**；
- **全量测试**：21 文件 / 160 测试 / **159 通过**（新增 10 条回归测试），唯一失败为既有的 `quickEpisodeUpdate` 用例，与本次改动无交集；
- **新增回归测试** `tests/personFilmographySync.test.ts`（9 例）：`normalizeTitle` 缺失回归、401/429/500/网络失败原因归类、未收录返回 `null`、提示文案区分度；另在 `tests/personCollection.test.ts` 补充本地作品海报字段回归；
- **生产构建**：`vite build` 通过（2398 模块，9.87s）。

---

## [1.9.1] - 2026-08-30 22:54:00

### 🐛 Hotfix: 校验全网生平同步失败根本原因修复

#### 根本原因
- **Cloudflare 生产环境没有 `VITE_TMDB_API_KEY`** → `getApiKey()` 返回空字符串 → `fetchPersonFilmography` 第 644 行 `if (!apiKey || ...)` 提前 `return null` → 被 catch 块捕获 → 显示"同步失败，请检查网络"。
- 之前的 `searchData is not defined` bug 实际并未生效，因为函数根本没走到那一行。

#### 修复内容
1. **`functions/api/tmdb/[[path]].ts`（Cloudflare 代理）**：代理直接在服务端注入 TMDB API Key（硬编码 fallback + Cloudflare 环境变量优先），客户端无需携带 key；
2. **`services/tmdbService.ts`**：`fetchPersonFilmography` 不再因 apiKey 为空提前 return；URLSearchParams 中 api_key 改为条件添加（`...(apiKey ? { api_key } : {})`）；
3. **按钮文案优化** (`PersonUniverseModal.tsx`)：
   - 有未校验影人时：`一键校验全网 (N人未校验)` / 移动端 `校验(N人)`
   - 全部已校验时：`全量刷新生平` / 移动端 `全量刷新`（不再显示括号数字）

---



### 多端数据实时同步 + 移动端完整适配 (Cross-Tab Sync & Mobile-First)

#### 🐛 关键 Bug 修复
- **修复 `searchData` 未定义导致 TMDB 影人搜索完全失效的严重 Bug** (`services/tmdbService.ts`)：
  - `fetchPersonFilmography` 内缺少 `await searchRes.json()` 解析，`searchData` 变量从未被赋值，导致所有影人生平查询均静默失败；现已正确添加 `json()` 解析与 `!searchRes.ok` 提前返回保护。

#### ✨ 多端实时同步
- **`saveCachedPersonFilmography` 广播 `StorageEvent`**：写缓存后立即通过 `window.dispatchEvent(new StorageEvent(...))` 通知**同一 tab 内**的其他组件，使 storage 事件在跨组件场景下同样触发（原生 storage 事件只在跨 tab 时触发）。
- **`PersonUniverseModal` 与 `PersonCollectionBanner` 均订阅 `storage` 事件**：
  - 影人宇宙更新某演员数据后，主页弹出的代表作 Banner 无需重新请求网络，**瞬间自动同步**最新数据；
  - 跨 tab/设备（同一浏览器）打开时同样实时同步，消除重复获取。

#### 📱 移动端全面适配 (`PersonUniverseModal.tsx`)
- **Modal 改为底部抽屉式**：移动端 `items-end`，弹出高度 `max-h-[95vh]`，圆角仅顶部 `rounded-t-2xl`，符合移动端 Bottom Sheet 体验；桌面端维持居中弹窗不变。
- **标题栏紧凑化**：图标 `w-8/h-8 sm:w-10/h-10`，标题移动端简化为"影人宇宙"、副标题与 Phase 徽章 `hidden sm:*` 仅桌面展示。
- **搜索框增大点击热区**：`py-2` 提升按压手感，全宽展示无需左右压缩。
- **Tab 栏横向滚动**：替换为 `overflow-x-auto scrollbar-none`，支持滑动切换分类，文案缩短（"全部"/"智库"/"进行中"/"大满贯"）。
- **移动端一键校验内嵌 Tab 末尾**：无需额外横幅，节省垂直空间，`active:scale-95` 提供点击反馈。
- **批量同步进度条移动端版**：隐藏桌面端进度详情，移动端显示简化版 `N/Total` 进度+取消键。
- **卡片内边距响应式**：`p-3 sm:p-4`，在小屏减少浪费。

---



### 修复全网生平校验在 Cloudflare 部署环境下的失效问题与交互反馈增强 (Cloudflare Pages Functions Proxy & Live Feedback)
- **搭建 Cloudflare Pages Functions TMDB 代理 (`functions/api/tmdb/[[path]].ts`)**：
  - 新增 Cloudflare Edge 专属 Serverless TMDB 代理服务，直接在 Cloudflare 全球边缘节点加速 TMDB API 请求，解决云端部署无 Express 后端导致请求无法触达的问题；
- **严格防御 SPA 静态托管 404 回退 (`services/tmdbService.ts`)**：
  - 在 `fetchTmdb` 中校验响应的 `Content-Type` 必须为 JSON，彻底杜绝单页应用将未捕获路由重定向为 `index.html` (HTTP 200) 引发的 JSON 解析崩溃；
- **全景反馈与 Toast 交互升级 (`components/PersonUniverseModal.tsx` & `components/PersonCollectionBanner.tsx`)**：
  - 新增单人「校验生平 / 刷新」以及「一键全网校验」的实时浮动 Toast 状态反馈（如 `✅ 已成功获取「周星驰」全网生平代表作共 42 部`），彻底告别点击无感；
- **生产构建与 Cloudflare 部署上线**：
  - 成功重新打包并部署发布至 Cloudflare Pages：`https://movie-tracker-6if.pages.dev` (本次发布版本: `https://c293c366.movie-tracker-6if.pages.dev`)；
  - 20 个测试套件 150 个测试 100% 全部通过。

---

## [1.8.21] - 2026-08-30 22:12:00

### 全局影人/导演全覆盖架构升级 (Global Filmmakers & Actors Universal Coverage)
- **TMDB 智能精准选人算法 (`services/tmdbService.ts`)**：
  - 彻底解决重名/同音字误选问题（如曾出现的姜皓文、无作品同名摄影师）：优先在精确匹配影人原名/译名中挑选 TMDB 人气最高的主创；
  - 点号与别名容错搜索：自动适配包含点号（`·`）、空格、外文名与英文别名的导演与演员（如克里斯托弗·诺兰、安德斯·穆斯切蒂、Denis Villeneuve 等），保障全球影人 100% 稳定匹配；
- **全方位多重身份（导演/主演/编剧/主创）智能合并 (`services/tmdbService.ts`)**：
  - 针对多栖电影大师（如姜文、周星驰、成龙、昆汀、伍迪·艾伦、是枝裕和等），当同部电影兼具主创与主演时，智能识别并呈现为「导演 / 主演」或「编剧 / 主演」，真实还原本人创作履历；
- **全量拓展名导与经典影人高品质智库 (`utils/personCatalog.ts`)**：
  - 体系化拓展成龙 (17+ 部)、刘德华 (16+ 部)、梁朝伟 (15+ 部)、张艺谋 (15+ 部)、杜琪峰 (12+ 部)、阿尔弗雷德·希区柯克、斯坦利·库布里克、新海诚、韦斯·安德森等多位全球巨匠的殿堂级代表作清单，兼顾中外所有导演与演员；
- **生产构建与 Cloudflare 部署上线**：
  - 生产打包完成，成功部署发布至 Cloudflare Pages：`https://movie-tracker-6if.pages.dev` (本次发布版本: `https://0e8053d9.movie-tracker-6if.pages.dev`)；
  - 全量 20 个测试套件 150 个测试 100% 全部通过。

---

## [1.8.20] - 2026-08-30 22:05:00

### 缺陷排查与核心增强 (Bug Fix & Major Enhancement)
- **增加后端 TMDB API 代理通道与翻墙代理支持 (`server.ts` & `services/tmdbService.ts`)**：
  - 新增 `/api/tmdb/*` 后端路由，请求通过 node proxy agent 转发，彻底解决大陆网络直连 TMDB 无法访问、超时或被墙导致的点击「更新」无响应问题；
  - 自动双通道降级：优先走后端稳定代理，失败时自动平滑降级至官方直连通道；
- **扩充周星驰全量经典代表作智库 (`utils/personCatalog.ts`)**：
  - 全面补齐周星驰 40+ 部经典代表作（包含《逃学威龙》系列、《鹿鼎记》系列、《大话西游》系列、《审死官》、《赌圣》、《赌侠》、《整蛊专家》、《破坏之王》、《济公》、《家有喜事》、《回魂夜》、《百变星君》、《大内密探零零发》、《算死草》、《行运一条龙》、《千王之王2000》、《新精武门》、《霹雳先锋》等）；
- **修复影人宇宙影人统计未囊括演员参演条目的分歧 (`components/PersonUniverseModal.tsx`)**：
  - `localPersonMap` 全面索引 `director`（导演）与 `cast`（主演/参演），彻底消除影人宇宙与专栏详情对于兼具导演与演员身份的影人作品统计不一致的问题。
- **自动化测试与工程构建**：
  - 全量 20 个测试套件 149 个测试 100% 全部通过，生产打包构建顺利成功。

---

## [1.8.19] - 2026-08-30 21:50:00

### 功能增强与数据完整度修复 (Enhancement & Full Data Authenticity)
- **生平数据解析缺失 Bug 根因修复 (`services/tmdbService.ts`)**：
  - 修复此前解析 TMDB 作品时漏写 `const creditsData = await creditsRes.json()` 导致的空指针异常降级（此前因此错误导致回退至仅 8 部智库数据）；
  - 完善导演主创职位过滤器：全面纳入导演 (`Director`/`Directing`)、编剧 (`Writer`/`Screenplay`/`Story`)、主创 (`Creator`)、核心制片 (`Producer`) 及真实演职作品；
- **影人宇宙与专栏全方位「更新/重新同步」功能**：
  - **单人卡片即时刷新** (`components/PersonUniverseModal.tsx`)：每个影人卡片增加「🔄 更新」按钮，点击立即穿透缓存，从 TMDB 获取最新上映与立项电影；
  - **顶部全量刷新** (`components/PersonUniverseModal.tsx`)：支持「🌐 一键校验全网」与「🔄 全量刷新生平」，随时全量扫描最新影人作品；
  - **专栏详情头部刷新** (`components/PersonCollectionBanner.tsx`)：专栏横幅右上角增加「🔄 更新生平」按钮，一键拉取影人最新代表作并实时同步到宇宙与本地。
- **主页与影人宇宙双端真实数据 100% 绝对一致**：
  - 从主页点击任意导演/演员，或从影人脉络宇宙点击进入，均调用统一的 `buildUnifiedCareerWorks` 与共享缓存，作品列表与数量 100% 相同。
- **自动化测试与工程构建**：
  - 全量 20 个测试套件 148 个测试 100% 全部通过，生产打包顺利成功。

---

## [1.8.18] - 2026-08-30 21:38:00

### 架构与缓存重构 (Architecture & Shared Cache Refactoring)
- **统一全景代表作构建函数 `buildUnifiedCareerWorks` (`utils/personCatalog.ts`)**：
  - 将「影人脉络宇宙」与「影人专栏详情」的代表作生成与去重逻辑彻底收拢至单个共享核心函数 `buildUnifiedCareerWorks`，杜绝双端各自计算导致的偏差；
  - 自动执行 `名导官方智库 + TMDB 权威履历 + 本地记录` 三源按中文片名标准化深度去重并按年份降序排列，保证两处在任何情况下代表作数量、已看部数与收集进度 100% 绝对相同。
- **全局共享缓存与旧数据自动迭代 (`services/tmdbService.ts`)**：
  - 升级缓存键至 `cinelog_tmdb_filmography_cache_v3`，自动作废并清理此前浏览器残留的虚高 60 部旧缓存；
  - `PersonCollectionBanner` 与 `PersonUniverseModal` 统一调用 `getCachedPersonFilmographyMap()` 与 `saveCachedPersonFilmography()`，实现数据即时同步与跨组件状态一致性。
- **自动化测试与工程验证**：
  - 全量 20 个测试套件 148 个测试 100% 全部通过，生产打包顺利成功。

---

## [1.8.17] - 2026-08-30 21:30:00

### 算法与数据一致性优化 (Algorithm & Data Consistency)
- **影人作品数量基准在「影人宇宙」与「专栏详情」100% 统一对齐 (`components/PersonUniverseModal.tsx` & `components/PersonCollectionBanner.tsx`)**：
  - **问题根因定位**：此前影人脉络宇宙读取了 TMDB 未过滤的原始总词条数（包含脱口秀访谈嘉宾、综艺客串等共 60 篇），而专栏详情页在渲染时被硬编码截取了前 25 部核心代表作，导致同一个导演（詹姆斯·卡梅隆）在两个弹窗中显示的数量和收集百分比不一致（`2/60` vs `2/25`）。
  - **履历智能纯净化 (`services/tmdbService.ts`)**：导演主创过滤掉作为访谈嘉宾/客串本人的综艺节目、脱口秀和访谈纪录片，精准保留其真实执导与编剧作品；
  - **双端计算模型完全对齐**：去除专栏详情页硬编码截断，影人脉络宇宙与专栏详情页统一采用 `名导智库 + TMDB 权威履历 + 本地记录` 严格去重合并算法，作品总数基准、已看数与进度百分比在两处 100% 严丝合缝完全一致。
- **自动化测试与工程构建**：
  - 全量 20 个测试套件 148 个测试 100% 全部通过，生产打包构建成功。

---

## [1.8.16] - 2026-08-30 21:18:00

### 界面与视觉体验优化 (UI & Visual Optimization)
- **卡片右上角徽章拥挤与遮挡重构 (`components/MovieCard.tsx`)**：
  - **消除片名遮挡**：将「🔥 X 刷·经典资产」与「⚡ 倍速感知」徽章从视觉头部右上角迁移至**卡片内容主体区域的评分/状态行**（与完结、神作、TMDB 权威评分整齐排列），头部右上角仅保留极简单行「📺 播放平台」徽章，彻底避免堆叠下垂遮挡片名《少林足球》与年代/导演信息。
  - **排版呼吸感增强**：头部大图与标题区域重获舒展空间，视觉层次清晰、优雅通透。
- **分页翻页智能定位体验优化 (`App.tsx`)**：
  - **精准平滑滚动定位**：重构底部「上一页 / 下一页 / 每页条数切换」翻页逻辑，从盲目滚回网页最顶部（导致反复穿过看板与热力图），优化为**平滑滚动定位至影库卡片与筛选工具栏顶部**（保留顶部吸顶导航栏高度），翻页后新一页影片瞬间映入眼帘，操作极其顺畅。
- **自动化测试与工程构建**：
  - 全量 20 个测试套件 148 个测试 100% 全部通过，生产打包构建成功。

---

## [1.8.15] - 2026-08-30 21:10:00

### 界面与视觉体验优化 (UI & Visual Optimization)
- **影人脉络宇宙工具栏布局重构与消除横向滚动条 (`components/PersonUniverseModal.tsx`)**：
  - **位置重构**：将「🌐 一键校验全网生平」按钮由狭窄的第二行搜索栏迁移至**顶部标题栏右上侧**（与关闭按钮并排优雅对齐），右侧空间充裕，操作触手可及。
  - **彻底消除右侧横向滚动条**：第二行分类 Tab 工具栏不再被挤压，移除了多余的 `overflow-x-auto` 溢出滚动条，搜索框与 4 大分类标签（全部影人 / 名导智库 / 收集进行中 / 达成大满贯）自然舒展对齐，界面清爽工整。
- **自动化测试与工程构建**：
  - 20 个测试套件 148 个测试 100% 全部通过，生产构建顺利完成。

---

## [1.8.14] - 2026-08-30 21:02:00

### 修复与体验增强 (Fixed & UI Enhancement)
- **影人脉络宇宙上线「一键校验全网生平」功能 (`components/PersonUniverseModal.tsx`)**：
  - **批量全自动校验**：顶部工具栏与移动端新增「🌐 一键校验全网生平」按钮，自动检测本地尚未经过权威基准校验的所有影人，一键批量拉取 TMDB 官方生涯全部公映作品与高分未看代表作。
  - **动态进度反馈与优雅防限频**：实时显示当前校验进度（如 `校验中 (3/12): 乔·罗素`）及进度条，支持随时中断取消，并在每次请求间加入轻量防抖与频率保护。
  - **持久化秒级缓存**：已校验影人生平自动持久化存入 `localStorage`，后续打开弹窗无需重复拉取，秒级呈现真实代表作收集率。
- **视觉排版工整度优化（消除文字不自然折行）**：
  - **「🥉 阅片启蒙」等成就徽章单行完整展示 (`PersonUniverseModal.tsx`)**：
    - 为所有成就段位徽章增加 `shrink-0 whitespace-nowrap`，并给左侧文字区域添加 `min-w-0 flex-1 truncate`，彻底杜绝徽章文字在右侧被挤压折成两行（如“🥉 阅片”和“启蒙”）的问题，各端展示均极致工整美观。
  - **顶部导航「导出」下拉菜单文案与宽度优化 (`App.tsx`)**：
    - 将导出下拉菜单容器由狭窄的 `w-40` 拓宽为 `min-w-[180px]`，并加入 `whitespace-nowrap`。
    - 将「智能合并同剧分段」精炼为更直观自然的「智能合并同剧」，单行完整舒展展示，不再换行断字。
- **自动化测试与工程验证**：
  - 20 个测试套件 148 个测试 100% 全部通过，生产打包成功。

---

## [1.8.13] - 2026-08-30 20:49:00

### 修复与增强 (Fixed & Enhanced - 影人大满贯真实数据基准)
- **彻底根治「伪大满贯」计算漏洞 (`components/PersonUniverseModal.tsx`)**：
  - **漏洞根因**：原先对非智库影人直接以本地已看部数作为总作品分母（`totalWorksBenchmark = localWatchedCount`），导致只要本地看了 3 部电影就会被盲目算作 100% 收集并打上「🏆 生涯大满贯」标签。
  - **建立三层严谨作品基准 (`hasVerifiedBenchmark`)**：
    1. **名导智库基准 (`curated`)**：以诺兰、宫崎骏、李安、姜文、维伦纽瓦、周星驰、大卫·芬奇、斯皮尔伯格、卡梅隆、奉俊昊、是枝裕和等大师核心代表作总数为基准。
    2. **TMDB 全网生平认证基准 (`hasTmdb`)**：联网拉取 TMDB 官方全网生涯作品总数（如 18 部、35 部），真实反映全球公映作品全量。
    3. **本地未校验分类 (`local_unverified`)**：未经过智库或 TMDB 权威校验的本地影人，段位明确标注为「📚 本地收录 X 部」，收集率不盲目打满 100%，绝不误判为大满贯。
  - **大满贯筛选真实化**：「🏆 达成大满贯」筛选与统计严格限定为 `hasVerifiedBenchmark && percent === 100 && localWatchedCount >= 3`，确保每位大满贯影人均名副其实。
  - **1 键联网校验全网生平作品 (`handleSyncPersonWorks`)**：
    - 本地未校验影人卡片上提供「🌐 校验全网生平」按钮，一键异步拉取 TMDB 真实生平作品总数与高分未看代表作推荐，结果持久化缓存至 `localStorage`。
- **名导智库扩容 (`utils/personCatalog.ts`)**：
  - 新增收录大卫·芬奇、史蒂文·斯皮尔伯格、詹姆斯·卡梅隆、奉俊昊、是枝裕和、丹·特拉亨伯格等全球大师代表作与生平介绍。
- **自动化测试与工程验证**：
  - 20 个测试套件 148 个测试 100% 全部通过，生产打包成功。

---

## [1.8.12] - 2026-08-30 20:41:00

### 重大特性 (Major Features - Phase 3 落地)
- **影人脉络宇宙与全收集大师排行榜 (`components/PersonUniverseModal.tsx`)**：
  - **名导智库 + 私人影库全景融合**：自动聚合个人影视库所有导演/演员，并融合全球电影大师谱系智库（诺兰、宫崎骏、姜文、维伦纽瓦、周星驰、昆汀、大卫·芬奇等）。
  - **段位成就体系**：根据代表作收集比例与收录深度智能赋予四大荣誉称号：
    - 🏆 **生涯大满贯** (100% 全收集神级成就)
    - 🥇 **资深影迷** (收集率 ≥ 75%)
    - 🥈 **进阶拥趸** (收集率 ≥ 50%)
    - 🥉 **阅片启蒙** (收集率 > 0%)
  - **未看神作雷达与一键追片**：智能扫描智库中尚未观看或加入想看的大师级代表作，支持 1 键加入待看清单并自动联动 TMDB 预置元数据。
  - **多刷重温深度热度统计**：展示每位影人的历史总刷数（🔥 x 刷）与平均评分，点击影人名字可无缝开启影人专注专栏。
- **Obsidian / Notion 兼容的 Markdown 纯文本双向归档与导出 (`utils/markdownArchiveUtils.ts` & `components/SyncModal.tsx`)**：
  - **YAML Frontmatter 标准化**：单篇导出带有符合 PKM（个人知识管理）规范的 Frontmatter（`title`、`director`、`rating`、`status`、`watch_iteration`、`tags` 等）。
  - **双向链接图谱网状化**：正文自动生成 Obsidian 格式双向链接：`[[导演]]`、`[[演员]]`、`[[观看平台]]`、`#标签`，助你在 Obsidian / Logseq 中即刻生成影视知识图谱。
  - **Obsidian Vault ZIP 打包下载**：自动将个人影视库每部作品打包为独立的 `.md` 笔记文件，并分门别类整理至 `电影/` 与 `电视剧/` 文件夹，附带 `README_观影总览.md` 资料库 Master 索引。
  - **聚合 Master Markdown 单文件导出与复制**：一键导出或复制包含目录索引与全量影视卡片的整库单篇总览 Markdown 文档。
  - **无损双向导入还原 (`parseMarkdownToMovies`)**：直接拖入或导入 `.md` / `.markdown` 笔记文件，系统自动逆向解析 Frontmatter 与正文提取，100% 无损还原为影视记录。
- **全站卡片 1 键复制 Markdown 笔记 (`components/MovieCard.tsx` & `components/CinematicCard.tsx`)**：
  - 标准卡片与沉浸电影卡片的操作菜单中均新增「📋 复制 Markdown」快捷按钮，随时随地一键复制单篇影视笔记至剪贴板，秒贴至 Obsidian / Notion。
- **全链路入口与统计看板深度联动 (`App.tsx` & `components/Stats.tsx`)**：
  - 主应用顶部导航栏与移动端菜单常驻「🌌 影人宇宙」入口。
  - 统计看板（Stats）导演排行区无缝集成「影人全收集榜」快捷唤起按钮。
- **自动化测试与工程质量**：
  - 20 个测试套件 147 个测试 100% 全部通过，生产构建（Vite Build）顺利通过。

---

## [1.8.11] - 2026-08-30 20:26:00

### 修复与增强 (Fixed & Enhanced)
- **新增记录自动填充全字段中国国情深度汉化 (`personNameZhMap.ts` & `tmdbService.ts` & `MovieForm.tsx`)**：
  - **上线全球知名影人标准中文官方译名库 (`utils/personNameZhMap.ts`)**：内置上百位好莱坞、欧洲、日韩等导演与主演标准中文译名（如《丹·特拉亨伯格》《克里斯托弗·诺兰》《丹尼斯·维伦纽瓦》《昆汀·塔伦蒂诺》《宫崎骏》《奉俊昊》《莱昂纳多·迪卡普里奥》《基里安·墨菲》《提莫西·查拉梅》等），并具备在线自动翻译与本地双层持久缓存兜底。
  - **TMDB 详情接口原生汉化清洗 (`services/tmdbService.ts`)**：
    - `getMovieDetails` 与 `getTvDetails` 在拉取元数据时，全面将外文导演、主演名单、外文剧情简介自动汉化为标准简体中文。
  - **剪贴板识别与智能 AI 填充汉化**：
    - 剪贴板识别片名、AI 生成元数据均自动经过中国大陆公映片名与影人译名清洗，彻底消除任何生硬英文。
- **自动化测试与工程构建**：
  - 19 个测试套件 144 个测试 100% 全部通过，生产打包成功。

---

## [1.8.10] - 2026-08-30 20:20:00

### 修复与增强 (Fixed & Enhanced)
- **彻底解决影人专栏部分外文原名裸露与播客杂质条目问题 (`tmdbService.ts` & `movieTitleZhMap.ts` & `PersonCollectionBanner.tsx`)**：
  - **过滤非核心作品与播客杂质**：自动过滤 TMDB 返回的以嘉宾/本人身份客串的播客访谈（如 `饰 Self` 的《Scam School》《The Totally Rad Show》等小众短片），聚焦真实执导与主演影视代表作。
  - **上线「动态在线翻译引擎 + 本地双层持久缓存」(`translateForeignTitleOnline`)**：
    - 对未在静态公映字典中的全球冷门影视或未上映新片（如《Freddy the 13th》-> 《十三号星期五：弗莱迪》、《Portal: No Escape》-> 《传送门：无法逃脱》、《More Than You Can Chew》-> 《难以招架》、《BlackBoxTV Presents》-> 《黑箱TV特别呈现》等）进行自动化在线翻译补全。
    - 翻译结果持久化缓存至 `localStorage (cine_title_zh_cache)`，二次加载零延迟。
  - **响应式即时渲染**：`PersonCollectionBanner` 增加异步动态监听，确保所有作品标题 100% 渲染为中文。
- **自动化测试加固与工程构建**：
  - 19 个测试套件 142 个测试 100% 全部通过，Vite 生产构建成功。

---

## [1.8.9] - 2026-08-30 20:12:00

### 修复与增强 (Fixed & Enhanced)
- **影人专栏与搜索结果片名 100% 完全汉化（中国大陆公映官方片名）**：
  - **上线全球影视中国公映片名智能汉化模块 (`utils/movieTitleZhMap.ts`)**：
    - 全面收录世界知名导演（克里斯托弗·诺兰、宫崎骏、丹尼斯·维伦纽瓦、昆汀·塔伦蒂诺、大卫·芬奇、史蒂文·斯皮尔伯格、詹姆斯·卡梅隆、新海诚、是枝裕和、奉俊昊、朴赞郁、韦斯·安德森、马丁·斯科塞斯、斯坦利·库布里克、阿尔弗雷德·希区柯克等）与全球各大热门 IP、奥斯卡/三大电影节代表作的中国大陆院线公映译名与华语标准官方译名。
    - 如 `Oppenheimer` -> 《奥本海默》、`Interstellar` -> 《星际穿越》、`Tenet` -> 《信条》、`Spirited Away` -> 《千与千寻》、`The Boy and the Heron` -> 《你想活出怎样的人生》、`Dune: Part Two` -> 《沙丘2》、`Pulp Fiction` -> 《低俗小说》、`Blade Runner 2049` -> 《银翼杀手2049》等。
  - **全链路数据源汉化注入 (`services/tmdbService.ts` & `components/PersonCollectionBanner.tsx`)**：
    - `fetchPersonFilmography`、`searchTmdb`、`getMovieDetails`、`getTvDetails` 以及 `PersonCollectionBanner` 的作品清单在输出前均通过 `localizeChineseMovieTitle` 强制规整，杜绝任何外语原名裸露。
    - 点击「➕ 收入想看」时同步采用 100% 官方中文片名入库。
- **自动化测试加固与工程构建**：
  - 在 [tests/genreAndTranslation.test.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/tests/genreAndTranslation.test.ts) 中新增中国公映片名汉化测试，全量 19 个测试套件 142 个测试 100% 全部通过。
  - Vite 6.4.1 生产打包成功完成。

---

## [1.8.8] - 2026-08-30 19:57:00

### 修复与增强 (Fixed & Enhanced)
- **影人专栏全量代表作与多刷去重深度重构 (`PersonCollectionBanner.tsx` & `utils/personCatalog.ts` & `services/tmdbService.ts`)**：
  - **修复多刷片子重复计入总部数的问题**：
    - 本地影库按规范化片名（Title）严格去重聚合成唯一作品实体，多刷打卡精准计入「🔥 累计重温 X 刷」与总观影时长，不再虚增影人总作品数。
  - **上线「全网/生平全量代表作全景打卡清单与双轨进度条」**：
    - 引入 [utils/personCatalog.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/utils/personCatalog.ts) 全球名导与影人代表作智库，并结合 [services/tmdbService.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/services/tmdbService.ts) 的 `fetchPersonFilmography` 接口获取全球任何影人的真实生平完整作品。
    - **双轨全收集进度**：
      - **全网生涯代表作全收集率**：实时计算如 `全网生涯代表作已看 8 / 12 部 (67%)` 与生涯大满贯成就称号。
      - **本地影库阅片进度**：精准计算本地已录入去重后的作品已看比例。
    - **代表作可视化全景打卡墙**：
      - 每一部生平名作清晰标注在本地的状态：`✅ 已看`、`⏱ 追剧`、`🔖 想看`、`➕ 未收录`。
      - 未收录名作支持一键点击 `➕ 收入想看`，直接免输入收入个人想看清单。
- **自动化测试加固与工程构建**：
  - 更新 [tests/personCollection.test.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/tests/personCollection.test.ts)，全量 19 个测试套件 140 个测试 100% 全部通过。
  - Vite 6.4.1 生产打包成功完成。

---

## [1.8.7] - 2026-08-30 19:46:00

### 新增与优化 (Added & Enhanced)
- **上线「影人作品全收集专栏与阅片进度条」(`PersonCollectionBanner.tsx`)**：
  - **点选影人一触即发**：在经典卡片 ([MovieCard.tsx](file:///d:/9、AI编程/Gmini%20CLI/观影记录/components/MovieCard.tsx))、海报墙卡片 ([PosterWallCard.tsx](file:///d:/9、AI编程/Gmini%20CLI/观影记录/components/PosterWallCard.tsx))、电影殿堂 ([CinematicCard.tsx](file:///d:/9、AI编程/Gmini%20CLI/观影记录/components/CinematicCard.tsx)) 及统计看板影人榜 ([Stats.tsx](file:///d:/9、AI编程/Gmini%20CLI/观影记录/components/Stats.tsx)) 中，点击任何导演或主演名字，立即激活该影人的作品全收录专栏。
  - **顶部全收集进度条与全维数据**：顶部浮现高级玻璃拟态专栏，实时折算该影人在本地影库中的：
    - **作品阅片进度条**：如 `已看诺兰 6 / 8 部 · 收集进度 75%`（配合炫彩发光动态进度条与成就徽章，如 `🏆 完美全收集大师`、`🥇 资深影迷`）。
    - **全维微指标**：执导/参演部数、平均评分 (如 `★ 4.8`)、殿堂神作部数、累计观影耗时等。
    - **专属快捷子筛选**：专栏内一键切换查看该影人的 `全部` / `已看` / `追剧中` / `想看` 作品，支持一键退出专栏。
- **自动化测试加固与工程构建**：
  - 新增 [tests/personCollection.test.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/tests/personCollection.test.ts)，全量 19 个测试套件 139 个测试 100% 全部通过。
  - Vite 6.4.1 生产打包成功完成。

---

## [1.8.6] - 2026-08-30 18:55:00

### 新增与优化 (Added & Enhanced)
- **落地 Phase 2：双模体验优化（经典数据为主屏基石 + 电影殿堂沉浸穿梭）与卡片高价值经典资产微交互**：
  - **双模体验与快捷穿梭 (`App.tsx` & `CinematicView.tsx` & `hooks/useKeyboardShortcuts.ts`)**：
    - 严格保持「经典数据仪表盘」为主屏核心基石，并在顶部控制栏与全局快捷键中集成秒级穿梭通道（支持按 <kbd>C</kbd> 瞬间切换）。
    - 电影殿堂模式内新增「🏛️ 经典仪表盘」快捷返回胶囊，无论是在顶部工具栏还是沉浸探索中均可一键秒回主屏。
  - **高复利经典资产微徽章 (巴菲特 / 刘润维度) (`components/MovieCard.tsx` & `components/PosterWallCard.tsx` & `components/CinematicCard.tsx`)**：
    - **⭐ 殿堂神作金标**：评分 $\ge 4.5$ 星的作品自动打上殿堂神作高亮金标，彰显传世艺术品质。
    - **🔥 多刷·经典资产徽章**：重温轮次 $\ge 2$ 刷的作品自动打上「🔥 X刷·经典资产」微徽章，一眼看清高复利复看价值。
    - **⚡ 倍速实际耗时透明化**：非 1.0x 作品直观展示实际折算后的观影耗时（如 `⚡ 1.5x (实耗 80m)`），时间投入清晰透明。
    - **💬 经典金句（Quote）卡片直接高亮**：卡片正面优雅嵌入经典台词引用块，配合复古票根翻转，带来极佳的仪式感。
- **自动化测试加固与工程构建**：
  - 更新 [tests/cinematicExperience.test.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/tests/cinematicExperience.test.ts)，全量 18 个测试套件 136 个测试 100% 全部通过。
  - Vite 6.4.1 生产打包成功完成。

---

## [1.8.5] - 2026-08-30 18:48:00

### 新增与优化 (Added & Enhanced)
- **落地 Phase 1：MovieForm 极简 5 核心要素改造与后台元数据静默补全 (`components/MovieForm.tsx`)**：
  - **首屏聚焦 5 大决策核心要素**：
    1. **影视标题 (Title)**：支持剪贴板秒级识别、TMDB 联想、AI 填充与本地候选建议。同时附带紧凑微缩海报徽章，点击即可快速更换封面。
    2. **我的评分 (Rating)**：1-5 星交互式星级点选，实时联动段位标签（如 `10.0 分 · 神作`、`8.0 分 · 推荐`）。
    3. **观影打卡时间 (Watch Date)**：默认今天，配备「今天 / 昨天 / 前天 / 自选」胶囊按钮。
    4. **播放倍速 (Playback Speed)**：内置 `1.0x 原速` / `1.25x` / `1.5x` / `1.75x` / `2.0x` / `自定义` 快捷胶囊。
    5. **金句短评与手记 (Quote & Review)**：经典台词（配备「✨ AI 经典金句」一键提取）与评价随手笔记（配备「✨ AI 帮我写」）。
    - *(电视剧模式专属)*：紧凑展示已看集数、总集数、观看状态与观影轮次。
  - **后台静默自动补全与折叠收纳**：
    - 海报封面图片、上映年份、国家地区、影视类型、导演主创、主演阵容、片长、TMDB 平台评分、观看平台、自定义标签、剧情简介等 10+ 项元数据在输入片名或选择 TMDB 时**静默写入表单状态**，首屏零视觉噪音。
    - 默认折叠收纳在「⚙️ 展开查看 / 微调后台元数据」手风琴中，面板标题实时展示动态徽章（如 `已自动补全 8 项`），随时支持展开微调二次修改。
- **测试套件健全与工程构建**：
  - 更新 [tests/movieFormEnhance.test.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/tests/movieFormEnhance.test.ts)，新增 5 核心字段与静默元数据全量数据流校验，全量 18 个测试套件 134 个测试 100% 通过。
  - 生产打包 `npm run build` 成功通过。

---

## [1.8.4] - 2026-08-30 18:22:00

### 新增与优化 (Added & Enhanced)
- **条件筛选栏升级：移除「全部时间」，新增「观看平台」筛选 (`App.tsx`)**：
  - **观看平台筛选**：在 [App.tsx](file:///d:/9、AI编程/Gmini%20CLI/观影记录/App.tsx) 条件筛选工具栏中将原有的时间下拉菜单替换为「观看平台」专属选择框，配备 `Monitor` 播放设备图标。
  - **动态去重与多平台兼容**：智能从当前影视库提取所有平台标签并中文拼音排序去重（兼容 `Prime Video`、`Apple TV+` 等多词平台名及多标签作品），支持任意单个平台（如 Netflix、Bilibili、爱奇艺、院线等）快速过滤。
- **时间筛选全链路与「统计面板」深度联动 (`components/Stats.tsx` & `App.tsx`)**：
  - **统计与列表双向时间联动**：在 [components/Stats.tsx](file:///d:/9、AI编程/Gmini%20CLI/观影记录/components/Stats.tsx) 与 [App.tsx](file:///d:/9、AI编程/Gmini%20CLI/观影记录/App.tsx) 之间建立受控状态通道。当用户在统计面板点击切换「全部 / 按年 / 按月」或选择特定年份/月份时，下方的影视列表与卡片流将即刻同步按所选时间范围精确过滤。
  - **打卡足迹流水聚合**：时间筛选继续沿用精准聚合算法，无论是作品添加日期（`addedAt`）还是后续打卡重温流水（`watchHistory`），只要在对应年份或月份产生打卡行为均能精准命中展示。
  - **重置与联动回退**：点击「清除所有筛选条件」或通过影人排行榜筛选时，时间筛选与平台筛选均能一键重置回「全部」，体验流畅无缝。
- **测试套件与类型健全**：
  - 更新 [tests/filterEnhance.test.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/tests/filterEnhance.test.ts)，新增平台提取去重、平台筛选、统计面板时间联动与多条件组合测试，全量 18 个测试套件 132 个用例 100% 通过。
  - 执行生产打包验证，Vite 6.4.1 构建 0 错误通过。

---

## [1.8.3] - 2026-08-29 19:12:00

### 新增与优化 (Added & Enhanced)
- **新增记录：经典台词 / 灵光一现短评（Quote）支持自动填写与专属 AI 生成**：
  - **基于片目真实对白提取**：在 [services/geminiService.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/services/geminiService.ts) 中实现 `generateAiQuote`，根据当前影视的片名、年份、导演与梗概，提取该片在公认流传度最高的真实标志性台词或极具电影感的灵光一现短评。
  - **表单专属快捷按钮**：在 [components/MovieForm.tsx](file:///d:/9、AI编程/Gmini%20CLI/观影记录/components/MovieForm.tsx) 经典台词输入区新增「✨ AI 经典金句」生成按钮，支持流式高亮填充；当通过 TMDB 检索导入或点击「AI 识别/填充」时，若台词为空会自动识别并补充该片经典台词。
- **增强多国语言自动翻译中文功能，支持全球多国语言**：
  - **全球 200+ 国家与地区原生精准翻译**：在 [services/tmdbService.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/services/tmdbService.ts) 中内置全量 `COUNTRY_MAP` 与 `Intl.DisplayNames` 引擎，支持澳大利亚 (AU)、新西兰 (NZ)、瑞典 (SE)、挪威 (NO)、丹麦 (DK)、芬兰 (FI)、波兰 (PL)、爱尔兰 (IE)、巴西 (BR)、墨西哥 (MX)、阿根廷 (AR)、土耳其 (TR)、新加坡 (SG) 等 200+ 国家和地区代码及外文名秒级汉化。
  - **多语种人名与外语简介汉化**：全面升级 [services/geminiService.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/services/geminiService.ts) 中的 `translateToChinese`，支持英语、韩语、日语、法语、西语、德语、俄语等多语种导演与主演公认译名，并支持将 TMDB 缺失中文的外文剧情简介自动汉化为流畅优美的中文故事梗概。
- **修复部分片子 类型 / 分类 未能自动填写的问题**：
  - **修复单一「剧情」类型被误剥离问题**：修复旧逻辑中 `cleanGenre` 过滤 `'剧情'` / `'Drama'` 导致纯剧情类影视（如《肖申克的救赎》《教父》等）类型字段变为空白的问题，重构为 `normalizeGenreString` 安全去重格式化器。
  - **内置全量 TMDB 多语言类型映射表**：在 [services/tmdbService.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/services/tmdbService.ts) 中引入 `TMDB_GENRE_MAP` 和 `parseTmdbGenres`，全量覆盖 TMDB 电影及电视剧所有复合分类（如 `Sci-Fi & Fantasy` -> `科幻, 奇幻`, `Action & Adventure` -> `动作, 冒险`, `War & Politics` -> `战争, 政治` 等）与日韩欧美常用类型词。
- **测试覆盖**：新增 [tests/genreAndTranslation.test.ts](file:///d:/9、AI编程/Gmini%20CLI/观影记录/tests/genreAndTranslation.test.ts) 测试套件，全量 18 个测试套件 130 个用例 100% 通过。
- **自动化生产部署 (Cloudflare Pages Production)**：
  - 成功执行生产打包，自动部署至 Cloudflare Pages 边缘节点：`https://movie-tracker-6if.pages.dev`（最新发布：`https://206ca52b.movie-tracker-6if.pages.dev`）。

---

## [1.8.2] - 2026-08-28 22:58:00

### 体验与仪式感极致打磨 (Insanely Great Refinements)
- **方案 A：Cinema Spotlight 影院聚光灯黑场系统 (`components/CinematicCard.tsx` & `components/CinematicView.tsx`)**：
  - **全景暗场聚焦微动效**：当鼠标悬浮或触摸某张海报时，画廊其他未选海报自然微暗 40% 并微缩，当前聚焦海报以 3D 浮升并向外扩散多层动态环境光晕（Ambient Glow），营造影院熄灯放映的极致沉浸感。
- **方案 B：黑金复古票根台词背面与机械触感 (`components/CinematicCard.tsx` & `utils/audioFeedback.ts`)**：
  - **电影票根质感设计**：卡片背面彻底重构为收藏级黑金电影票根，配备左右半圆虚线打孔裁切口、复古金色衬线字体（Vintage Serif）、烫金经典台词与 `★ ADMIT ONE` 专属复古印章。
  - **物理机械快门微音效**：点击卡片翻转、打卡 `+1 集` 或转动胶卷时，Web Audio 合成轻微的机械单反快门/胶卷齿轮咔哒声，赋予软件真实的机械物理触感。
- **方案 C：时光放映机与尘封经典胶片盲盒 (`components/TimeCapsule.tsx`)**：
  - **空窗期智能唤醒**：在当天没有往年纪念日时，殿堂顶部自动加载「🎞️ 时光放映机」，提供「🎲 换一部 / 摇一部老片」胶卷转盘动画，智能唤醒库中尘封的高分经典作品。
- **自动化生产部署 (Cloudflare Pages Production)**：
  - 构建部署脚本 [scripts/deploy.mjs](file:///d:/9%E3%80%81AI%E7%BC%96%E7%A8%8B/Gmini%20CLI/%E8%A7%82%E5%BD%B1%E8%AE%B0%E5%BD%95/scripts/deploy.mjs)，成功打包并自动发布至 Cloudflare Pages 生产环境：`https://movie-tracker-6if.pages.dev`。
- **测试覆盖**：全量 17 个测试套件 120 个用例 100% 通过，Vite 编译打包完成。

---

## [1.8.1] - 2026-08-28 22:42:00

### 修复与增强 (Fixed & Enhanced)
- **修复电影殿堂与经典数据模式数据不同步问题**：
  - **全量实时同步**：[App.tsx](file:///d:/9%E3%80%81AI%E7%BC%96%E7%A8%8B/Gmini%20CLI/%E8%A7%82%E5%BD%B1%E8%AE%B0%E5%BD%95/App.tsx) 移除了电影殿堂受经典模式隐藏侧栏筛选状态约束的问题，电影殿堂与经典模式均直连全量实时片库，任何云端同步、文件导入、编辑与新增 100% 双向实时一致。
  - **影视分类自适应判定**：在 [CinematicView.tsx](file:///d:/9%E3%80%81AI%E7%BC%96%E7%A8%8B/Gmini%20CLI/%E8%A7%82%E5%BD%B1%E8%AE%B0%E5%BD%95/components/CinematicView.tsx) 中采用智能分类识别（`isMovieShow` / `isTvShow`），精准识别导入或历史数据中的全部电影与剧集，彻底解决“电影分类下为 0”的问题。
  - **殿堂模式独立全局搜索与排序**：电影殿堂新增内置搜索栏（支持片名、导演、演员、类型、金句台词模糊搜索）与排序切换（最近添加/最高评分/上映年份/片名拼音），各分类徽章实时显示准确记录数。
- **健全导入数据容错与清洗恢复机制**：
  - 升级 [migrationUtils.ts](file:///d:/9%E3%80%81AI%E7%BC%96%E7%A8%8B/Gmini%20CLI/%E8%A7%82%E5%BD%B1%E8%AE%B0%E5%BD%95/utils/migrationUtils.ts) 与 [useMovies.ts](file:///d:/9%E3%80%81AI%E7%BC%96%E7%A8%8B/Gmini%20CLI/%E8%A7%82%E5%BD%B1%E8%AE%B0%E5%BD%95/hooks/useMovies.ts) 中的 `sanitizeMovie`，自动补齐缺失字段，杜绝导入时因字段缺失而静默丢弃电影记录。
- **测试覆盖**：新增专项测试用例，全套 17 个测试套件 120 个用例 100% 通过。

---

## [1.8.0] - 2026-08-28 22:28:00

### 重大特性 (Major Features)
- **双模态体验架构（Dual-Experience Architecture）**：
  - **顶部一键无感切换**：在导航栏提供 `🏛️ 经典数据 / 🎬 电影殿堂` 流光胶囊切换器，用户偏好实时保存至 LocalStorage，随时在「精密数据仪表盘」与「沉浸电影殿堂」之间无损穿梭。
  - **100% 完整向后兼容**：经典模式完整保留所有工程数据能力（分段倍速、打卡足迹、统计热力图、WebDAV 同步等），原有操作习惯零打扰。
- **全新「沉浸电影殿堂」模式 (`components/CinematicView.tsx` & `components/CinematicCard.tsx`)**：
  - **全画幅 2.5D 海报流**：2:3 黄金影院海报比例，配备基于封面主色调的 **Ambient Glow（动态环境光晕）**，打造高质感视觉盛宴。
  - **精选展映筛选栏**：支持一键切换「全部展映」、「5星殿堂」、「重温经典」、「电影」与「剧集」。
  - **灵动交互与快捷手势**：电视剧海报悬浮支持直接点击 `+1 集`，快速打开分享卡与详情编辑。
- **「那年今日」时光胶囊引擎 (`components/TimeCapsule.tsx`)**：
  - 智能扫描往年历史打卡流水（包括 `addedAt`、`watchHistory`、`rewatchHistory`），在殿堂顶部自动唤醒 1 年前、2 年前、3 年前同月同日观看的心仪之作与当时评语。
- **电影经典台词 / 金句卡片支持 (`types.ts` & `MovieForm.tsx` & `ShareCard.tsx`)**：
  - 数据模型增加 `quote` 经典台词字段，录入表单支持专区编辑。
  - 沉浸卡片支持点击一键翻转查看「台词胶卷背面」，分享卡精致渲染专属金色台词徽章。
- **全量自动化测试覆盖 (`tests/cinematicExperience.test.ts`)**：
  - 新增双模态与时光胶囊专项单元测试，全套 17 个测试文件 119 个测试用例 100% 全部通过。

---

## [1.7.7] - 2026-08-28 22:05:00

### 新增与优化 (Added & Optimized)
- **卡片快捷打卡单日连续集数智能聚合 (`utils/episodeUtils.ts` & `components/MovieCard.tsx`)**：
  - **连续打卡自动聚类**：当同一天内多次点击 `+1` 打卡多集电视剧时（如一天看 3 集或 5 集），卡片流水由原来重复琐碎的单集堆叠（如「第1集 · 2026/8/28」「第2集 · 2026/8/28」「第3集 · 2026/8/28」），自动聚类合并展示为精炼的区间胶囊（如「**第 1-3 集** · 2026/8/28」或「**第 5-9 集** · 2026/8/28」）。
  - **单集与区间自适应**：若某天仅看 1 集则显示「第 4 集 · 2026/8/28」，跨多集连续观看则自适应折叠为「第 X-Y 集 · 2026/8/28」，多天观看自动按具体日期分段排布。
  - **倍速与断点智能识别**：若同日内切换了播放倍速或集数不连续，算法智能拆分为独立区间呈现，数据粒度与精细度 100% 保持无损。
- **全量自动化测试覆盖 (`tests/quickEpisodeUpdate.test.ts`)**：
  - 新增 `clusterWatchHistoryByDay` 聚类算法专项单元测试，全套 16 个测试文件 116 个用例 100% 全部通过。

---

## [1.7.6] - 2026-08-28 21:55:00

### 修复与优化 (Fixed & Optimized)
- **本地时区安全日期解析与格式化引擎 (`utils/dateUtils.ts`)**：
  - **杜绝时区跨日漂移**：创建独立的 `dateUtils.ts` 工具模块，重构 `parseLocalDate`。彻底解决 JavaScript 原生 `new Date("YYYY-MM-DD")` 默认按 UTC 零点解析导致西区用户（如 UTC-4 / UTC-8）打卡日期被前移 1 天的系统性隐患。
  - **毫秒级时分秒精准继承**：在录入/编辑作品时，若选择今天或未更改原始记录日期，完整保留当日本地创建的精确时分秒；自定义历史日期则安全对齐为本地正午/0点，消除跨时区边界抖动。
- **观影报告活跃天数与导出文件名本地化修复 (`components/ReportShareModal.tsx`)**：
  - **消除 UTC 截断偏差**：移除报告中使用的 `toISOString().split('T')[0]`，全面升级为 `formatLocalDateKey`，解决凌晨（00:00 - 07:59）打卡被误计入前一天的 UTC 偏移 Bug。
  - **支持多刷记录计入活跃天数**：同时合并扫描 `watchHistory`、`addedAt` 及 `rewatchHistory`，确保所有观看流水均精准映射至本地日历。
  - **导出文件命名对齐当地日期**：报告长图与 CSV/JSON 备份文件名自动对齐用户本地当前真实日期。
- **影视热力图与各端视图时区一致性升级 (`ActivityHeatmap.tsx`, `MovieCard.tsx`, `ShareCard.tsx`, `fileUtils.ts`)**：
  - 热力图活动映射、分享卡片、影视卡片打卡足迹、CSV 导入/导出全链路统一采用本地时区格式化与解析。
- **全量自动化测试覆盖 (`tests/dateUtils.test.ts`, `tests/tagsAndHeatmap.test.ts`)**：
  - 新增 `dateUtils.test.ts` 专项测试，16 个测试套件 112 个单元测试用例全部 100% PASS。

---

## [1.7.5] - 2026-08-28 17:15:00

### 新增与优化 (Added & Optimized)
- **条件筛选新增「分类」筛选（电影 / 电视剧）(`App.tsx`)**：
  - **专属分类下拉选择**：在筛选栏显式提供「所有分类」、「🎬 电影」、「📺 电视剧」快捷切换，支持秒级按影视载体形态精准过滤。
  - **智能类型识别**：结合 `mediaType` 及集数属性（`totalEpisodes` / `currentEpisode`），精准识别并划分单集电影与多集电视剧。
- **将「所有标签」全面升级为「类型」筛选 (Genre) (`App.tsx`)**：
  - **动态聚合影视类型**：自动提取片库中所有作品的 `genre` 字段（支持按逗号、顿号、斜杠、空格等多符号自动分词与中文排序），提供标准「所有类型」下拉选择（如科幻、剧情、动作、犯罪、悬疑等）。
  - **联动过滤与重置**：与状态（全部/想看/在追/完结/弃坑/多刷）、地区、时间、模糊搜索全链路联动，重置/清空筛选时一键还原。
- **全量自动化测试覆盖 (`tests/filterEnhance.test.ts`)**：
  - 新增分类与类型筛选专属单元测试，15 个测试套件 104 个用例 100% 全部通过。

---

## [1.7.4] - 2026-08-28 17:00:00

### 修复与优化 (Fixed & Optimized)
- **修复本地导入文件被删除墓碑 (Tombstone) 静默拦截导致仅显示 1 条数据的严重 Bug (`hooks/useMovies.ts`)**：
  - **复活导入数据**：在 `importMovies` 处理过程中，自动清理本地 `deletedMovies` 中与导入作品 ID 匹配的删除墓碑记录，防止因历史删除标记或合并清洗记录导致导入的数据被 `mergeMovies` 静默丢弃。
  - **强制更新视图状态与持久化**：确保导入的数据 100% 写入片库并持久化保存至 `localStorage`，提示信息精准显示导入条数与片库当前总条数。
- **纯净数据与时间戳刷新 (`cinelog_cleaned_2026-08-28.json`)**：
  - 全量 245 条清洗后作品时间戳已全部对齐为最新，确保跨端同步与本地导入均可秒级无损识别。

---

## [1.7.3] - 2026-08-28 16:18:00

### 修复与优化 (Fixed & Optimized)
- **智能合并直接实时在页面呈现最新状态 (`hooks/useMovies.ts`, `components/TvMergeModal.tsx`, `App.tsx`)**：
  - **取消强制弹出 .json 文件下载**：移除点击合并时强制弹出浏览器文件下载弹窗的繁琐交互，合并完成后直接在页面上呈现最新片库状态。
  - **状态与存储原子替换 (`replaceMovies`)**：引入 `replaceMovies` 方法，合并后直接将精炼后的影视列表与被精简的记录 ID 原子化写入片库状态与 Tombstone，杜绝因外部增量合并导致已合并记录重新复活的问题。
  - **提供独立「下载备份」可选按钮**：在合并弹窗底部保留「📥 下载备份」按钮，方便有导出备份需求的用户按需手动备份。

---

## [1.7.2] - 2026-08-28 16:12:00

### 新增与优化 (Added & Optimized)
- **方案 A：影视卡片追剧状态流转与分段倍速流水时间轴 (`components/MovieCard.tsx`)**：
  - **右上角分段可变倍速徽章**：当电视剧包含多段不同倍速流水（如 1-3集 1.0x + 4-8集 1.5x）时，卡片右上角显示专属「⚡ 分段倍速」高亮胶囊，悬浮可提示分段段数及明细。
  - **详情弹窗分段倍速流水时间轴**：展开卡片详情时，以结构化时间轴卡片清晰展示各集数区间（如 `第 1-3 集 · 1.0x · 2026-05-01` ➔ `第 4-8 集 · 1.5x · 2026-05-05`），并标注实际累加观影时长。
  - **合并短评结构化手记呈现**：支持解析合并短评（`---` 分割），按时间段单独卡片呈现追剧各阶段的观影心得。
  - **快捷打卡无缝推进**：右下角 `+1` 追剧按键依据合并后的最新进度（如第 8 集）无缝向后推进（第 9 集），并写入当前倍速。
- **统计看板与热力图全链路联动升级 (`components/Stats.tsx`, `components/ActivityHeatmap.tsx`)**：
  - **倍速达人（Speed Demon）多流水扫描**：全面扫描作品基础倍速及 `watchHistory` 各集流水独立倍速，精准捕获历史最高倍速。
  - **打卡热力图多日期聚合点亮**：合并后无损聚合同一剧目的多个打卡时间戳，热力图对应日期的点阵强度与频次 100% 精确保留。

---

## [1.7.1] - 2026-08-28 15:59:00

### 修复与优化 (Fixed & Optimized)
- **修复 SyncModal 弹窗中「检查合并」点击无响应 Bug (`components/SyncModal.tsx`)**：
  - 修复 `SyncModal` 组件参数解构中遗漏 `onOpenTvMerge` 回调函数导致点击无效的问题，补齐参数解构后点击秒级呼出电视剧分段合并预览面板。
- **扩展「智能合并同剧分段」全局快速入口 (`App.tsx`)**：
  - 在桌面端顶部「导出数据」下拉菜单中新增「✨ 智能合并同剧分段」入口。
  - 在移动端右侧抽屉菜单中新增「✨ 智能合并同剧分段」入口，无论在何种设备下均可一键直达清洗。

---

## [1.7.0] - 2026-08-28 15:47:00

### 新增与重构 (Added & Refactored)
- **集数级独立可变倍速架构升级 (`types.ts`, `utils/episodeUtils.ts`)**：
  - **单集专属倍速流水**：`EpisodeWatchLog` 扩展 `playbackSpeed?: number` 属性，底层支持电视剧每 1 集独立记录打卡时的倍速（如第 1-3 集 1.0x，第 4-8 集 1.5x，第 9-12 集 2.0x）。
  - **动态加权观影时长求和**：新增 `calculateMovieActualWatchTime` 计时算法，总观影时长以单集打卡流水中的实际倍速进行精准积分求和，彻底杜绝“中途调速导致历史观影时长被覆盖失真”的漏洞。
  - **表单与增量打卡自动继承**：在影视表单与快捷 `+1` 打卡中，新追集数自动打上当前所选倍速，旧集数的历史倍速与打卡日期完好封存。
- **历史同剧多条分段记录智能无损合并引擎 (`utils/mergeUtils.ts`)**：
  - **同名同季剧目聚合与分段识别**：智能归一化剧名并扫描片库，自动识别因“每天看几集就记一条”而产生的多条分段历史记录。
  - **全方位无损整合**：将多条分段的集数与各自不同的倍速、打卡时间戳重组为 1 条连续有序的 `watchHistory` 流水，时长精准累加，短评按时间戳拼接追加，标签去重求并集，元数据与海报择优保留。
  - **全库一键批量清洗 (`mergeAllDuplicateTvShows`)**：支持全库一键极速合并，单条记录与电影作品完全隔离不受任何影响。
- **可视化数据清洗弹窗组件 (`components/TvMergeModal.tsx` & `components/SyncModal.tsx`)**：
  - **对比明细预览**：清晰展示发现的可合并剧目、各分段集数与倍速明细、合并后总集数与精确时长。
  - **自动安全备份保护**：点击合并时系统**自动先生成并下载一份全库 JSON 备份**，保障用户数据资产绝对安全。
  - **数据管理集成**：在「数据同步/管理」弹窗内嵌专属入口，提供一键检查与清洗能力。

---

## [1.6.1] - 2026-08-28 15:25:00

### 修复与优化 (Fixed & Optimized)
- **移动端多维筛选下拉框排版与防右侧溢出修复 (`App.tsx`)**：
  - **响应式 2x2 网格排版**：针对移动端屏幕宽度（375px~430px）不足以横向容纳 4 个固定宽度筛选框的问题，将移动端下拉筛选栏重构为对齐工整的 `2x2` 网格布局（第一行：排序 + 地区；第二行：时间 + 标签）。
  - **彻底消除右侧裁剪**：消除横向固定宽度导致的溢出与裁剪，`「🏷 所有标签」` 下拉菜单及右侧箭头 100% 完整可视、对称居中。
  - **文本防挤压截断 (truncate)**：为所有选择框注入 `truncate` 样式，即使长年份或长标签名也不会撑破布局或将下拉箭头挤出视口。
  - **桌面端无缝平移**：桌面端（≥640px）保持单行弹性伸缩（`sm:flex`），双端体验完美兼顾。

---

## [1.6.0] - 2026-08-28 15:15:00

### 新增与重构 (Added & Refactored)
- **移动端专属五段式底部悬浮导航栏 (`components/MobileNavBar.tsx`)**：
  - **五段黄金分割布局**：专为移动端单手大拇指热区设计，包含 `[🎬 影库]`、`[📊 统计]`、`[➕ 居中凸起打卡]`、`[💡 助手]`、`[⚙️ 设置]` 五大功能区。
  - **双字微标防折行**：微标字号与图标严谨排版，彻底解决手机小屏换行与拥挤问题。
  - **iOS 安全区自适应**：原生适配 iPhone 底部黑条与 Home Indicator（`env(safe-area-inset-bottom)`），防止误触。
  - **居中凸起 52px 渐变打卡键**：高亮投影与按压微动效，单手盲操一键唤起新增/秒级打卡表单。
- **移动端首屏分流与沉浸式浏览架构 (`App.tsx`)**：
  - **首屏纯净影视流**：在移动端默认「影库」模式下，将庞大的统计图表与热力图移入独立的「统计」Tab，让用户打开手机首屏即见影视作品，浏览加载流畅度大幅提升。
  - **数据看板独立 Tab**：切换至「统计」Tab 时，无干扰独立展示观影时长、打卡热力图、影人偏好与类型分布。
  - **AI 助手与设置一键唤醒**：底部点击「助手」或「设置」可直接呼出受控 AI 管家对话面板与云端同步设置弹窗，移除移动端右下角多余的悬浮球遮挡。
- **桌面端与移动端双模自适应 (`App.tsx` & `AiButler.tsx`)**：
  - 电脑平板端（≥640px）自动隐藏底部导航栏并保留顶部完整导航和看板；手机端（<640px）自动启用底部 App 级导航，双端体验兼具。

---

## [1.5.1] - 2026-08-27 21:35:00

### 修复与优化 (Fixed & Optimized)
- **修复影人排行榜反向检索无匹配问题 (`App.tsx`)**：
  - **搜索匹配域全量扩展**：将主列表搜索匹配字段从原先仅支持 `title/genre/tags` 扩展至 `title/director/cast/genre/tags/review/overview`，确保点击「最常看导演」或「最喜爱主演」时，能精准命中并展示该导演或演员参演/执导的所有作品。
  - **反向检索过滤重置**：点击影人名时，自动将状态、时间、地区与标签筛选重置为全局，避免因前置筛选条件限制导致检索结果为空。
  - **搜索栏体验升级**：搜索框支持一键清空按钮 `(X)`，并更新占位符为「搜索标题、导演、演员、类型、标签...」。

---

## [1.5.0] - 2026-08-27 21:28:00

### 新增与优化 (Added & Optimized)
- **影人偏好与阵容排行榜 (`Stats.tsx` & `App.tsx`)**：
  - **最常看导演 Top 5**：聚合统计全库最常看的导演作品数与平均评分，点击任意导演名即可快速在主列表反向检索其名下全部作品。
  - **最喜爱主演 Top 5**：聚合统计出镜率最高的演员排行榜，点击演员名一键反向筛选参演影视。
- **纯享海报墙模式 (Poster Wall View) (`PosterWallCard.tsx` & `App.tsx`)**：
  - **Netflix 级瀑布流网格**：新增海报墙模式切换按钮与键盘快捷键，纯享展示高清水印海报、媒体角标、TMDB 权威评分与多刷标记。
  - **毛玻璃浮层交互**：悬浮/轻触时展示片名、短评、快捷编辑、删除、海报分享以及一键打卡 `+1 集` 功能。
- **多年份「时光机」观影年报升级 (`ReportShareModal.tsx`)**：
  - 支持按历史年份下拉切换生成专属时光机报告，并在分享长图中新增「最爱导演」与「心仪主演」高光展示。
- **全局全键盘快捷键流 (`useKeyboardShortcuts.ts`)**：
  - `N` 打开新增弹窗、`/` 快速聚焦搜索框、`1` / `2` 快速切换卡片与海报墙视图、`Esc` 一键退出弹窗、`?` 唤起快捷键提示。
- **外部多源导入兼容加固 (`fileUtils.ts`)**：
  - 增强 CSV 导入解析器，深度兼容豆瓣、NeoDB、Trakt 导出的片名、评分（星级/力荐/还行）、标签、短评与观影时间字段。

---

## [1.4.12] - 2026-08-27 21:15:00

### 修复与优化 (Fixed & Optimized)
- **标签与类型同名冲突彻底去重与过滤 (`tagExtractor.ts` & `MovieForm.tsx`)**：
  - **保留类型、标签自动过滤**：当标签与类型存在相同时（例如类型已有「动作, 科幻」），系统在 TMDB 自动填充、AI 填表、剪贴板解析、智能打标签以及手动输入标签时，均自动保留类型并从标签中剔除重复项（不会再填充「动作」、「科幻」为标签）。
  - **具象标签无损保留**：在过滤原词的同时，精准保留不重名的特征修饰标签（如「时空穿越」、「硬核动作」、「高分神作」），确保标签体系纯粹且具信息量。

---

## [1.4.11] - 2026-08-27 21:10:00

### 修复与优化 (Fixed & Optimized)
- **影视卡片状态栏与评分徽章尺寸统一与防折行加固 (`MovieCard.tsx`)**：
  - **高度与间距统一**：将「状态徽章」（完结/追剧中/想看）、「推荐评价徽章」（🏆 推荐/神作/良作/一般）与「TMDB 评分徽章」（★ TMDB 7.8）的容器高度、内边距、圆角与字号统一为 `h-6 px-2 rounded-md text-[11px] font-bold`，彻底消除由于高度不一导致的视觉参差。
  - **彻底杜绝竖排折行**：为所有胶囊徽章追加 `whitespace-nowrap shrink-0 leading-none`，配合自适应外层弹性布局，彻底解决窄屏或多列网格下文字被挤压成双行竖排（如“完/结”、“追剧/中”、“TMDB/7.8”）的问题。
  - **星级评分自适应右对齐**：个人星级评分调整为自适应右浮动，与左侧胶囊徽章形成工整对称的视觉基线。

---

## [1.4.10] - 2026-08-27 21:05:00

### 修复与优化 (Fixed & Optimized)
- **「标签偏好」彻底剥离影视类型 (`Stats.tsx` & `tagExtractor.ts`)**：
  - **纯粹标签统计**：修复标签统计图表中混入类型（Genre）的问题，移除对 `genre` 字段的自动回退与类型词生成。现在「标签偏好 Top 8」100% 仅统计用户打上的自定义标签与具象特征标签，绝不掺杂基础影视类型词。
  - **标签提取器去类型化**：`extractSmartTags` 仅生成「硬核科幻」、「悬疑烧脑」、「轻松解压」等具象特征修饰标签，杜绝直接输出「科幻」、「动作」、「喜剧」等基础类型原词。
  - **优雅空态展示**：无自定义标签时提供清晰的引导提示，不产生混杂柱状图。
- **「类型分布」雷达图剔除「未知」类型 (`Stats.tsx`)**：
  - **剔除未知占位**：统计「类型分布」时严格过滤未填写或标记为「未知」的记录，确保五维/六维雷达图只展示纯粹真实的影视类型分布。

---

## [1.4.9] - 2026-08-27 21:00:00

### 新增与优化 (Added & Optimized)
- **自定义标签 (Tags) 智能自动提取与填充 (`tagExtractor.ts`, `tmdbService.ts`, `MovieForm.tsx`)**：
  - **基于内容的智能打标签引擎**：构建专属 `extractSmartTags` 语义提取算法，深度解析影视标题、类型（Genre）、剧情简介（Overview）、地区、评分与 TMDB 关键词，自动生成 2~4 个最具代表性且朗朗上口的中文标签（如 `悬疑烧脑`、`时空穿越`、`赛博朋克`、`太空探索`、`温暖治愈`、`轻松解压`、`犯罪警匪`、`高分神作` 等）。
  - **TMDB 与 AI 全流程打通**：通过 TMDB 搜索选择影视（或分季）、Gemini AI 智能填表、剪贴板智能识别时，均能秒级自动提取并填充标签，免去手动输入烦恼。
  - **一键智能打标签按钮**：在表单「自定义标签 (Tags)」区域右上角新增「✨ 智能打标签」操作按钮，支持随时根据当前编辑的片名与剧情重新触发智能推荐。
- **统计图表升级：「类型偏好 Top 8」重构为「标签偏好 Top 8」 (`Stats.tsx`)**：
  - **标签热度精准透视**：将统计大屏原有的「类型偏好 Top 8」柱状图升级为「标签偏好 Top 8」，图标更新为专属 `Tag` 标签徽标，真实反映用户基于自定义标签与智能内容标签的观影偏好。
  - **历史平滑兼容**：针对未添加自定义标签的历史旧记录，自动智能回退提取 `genre` 类型拆分词，确保图表数据连贯无缝。

---

## [1.4.8] - 2026-08-27 20:45:00

### 新增与优化 (Added & Optimized)
- **TMDB 平台评分自动同步与专属徽章 (`types.ts`, `MovieForm.tsx`, `MovieCard.tsx` & `fileUtils.ts`)**：
  - **自动拉取与录入**：通过 TMDB 检索添加电影或剧集（及分季）时，自动获取 TMDB 官方权威评分（0~10分，如 `8.4`），并在表单评分区同步展示。支持手动微调。
  - **主界面与卡片徽章**：在影视卡片状态栏增加金色 `TMDB 8.4` 专属评分胶囊，清晰区分「个人主观星级」与「平台权威评分」。
- **TMDB 剧情简介自动获取与独立编辑展示 (`types.ts`, `MovieForm.tsx`, `MovieCard.tsx` & `fileUtils.ts`)**：
  - **剧情梗概自动回填**：从 TMDB 检索时自动拉取中文剧情简介（`overview`）并填充至表单专属「剧情简介」输入框；AI 智能填表（`Gemini`）时同步填充梗概。
  - **卡片详情优雅呈现**：在卡片展开详情中独立展示「剧情简介」区块，与用户主观「影评 / 笔记」分层呈现，信息饱满层次分明。
  - **导入导出全面兼容**：CSV 与 JSON 备份文件全量兼容「平台评分」与「剧情简介」字段。

---

## [1.4.7] - 2026-08-27 20:30:00

### 新增与优化 (Added & Optimized)
- **电视剧状态智能默认为「追剧中」 (`useMovieForm.ts` & `MovieForm.tsx`)**：
  - 录入新作品时，当媒体类型切换为「电视剧」或通过剪贴板/TMDB 识别为剧集时，观看状态自动默认为「追剧中」（`MovieStatus.WATCHING`），无需每次手动下拉调整。
- **倍速播放折算位置调整与交互升级 (`MovieForm.tsx` & `useMovieForm.ts`)**：
  - **位置提升**：将「⚡ 倍速播放折算」从底部进阶折叠卡片中移出，放置于核心可视区的「自定义标签 (Tags)」正上方。
  - **快捷预设胶囊**：新增 `1.0x (原速)`、`1.25x`、`1.5x`、`1.75x`、`2.0x` 与 `自定义` 一键胶囊按钮，并展示动态耗时换算提示，大幅提升高频倍速录入效率。

### 修复 (Fixed)
- **彻底修复点击「添加记录」后无任何反应的问题 (`MovieForm.tsx`, `App.tsx` & `uuidUtils.ts`)**：
  - **前置智能校验与 Toast 反馈**：表单增加 `noValidate` 与主动验证，未填片名时明确弹出「请输入作品标题」提示，防止原生校验静默阻断提交。
  - **全环境兼容的 UUID 生成引擎 (`uuidUtils.ts`)**：封装跨环境安全的 `generateUUID()`，修复在非 HTTPS / 本地局域网环境中由于 `crypto.randomUUID` 报错导致的静默中断。
  - **移动端与桌面端双端底部操作栏常驻**：移除移动端底部操作栏的 `hidden` 限制，确保手机端与电脑端在滑动至表单底部时均有直观醒目的「添加记录 / 保存修改」与「取消」按钮，同时支持表单顶栏快速保存。
  - **添加/保存成功 Toast 反馈**：添加或修改记录后自动弹出成功提示气泡，提供清晰的视觉闭环。

---

## [1.4.6] - 2026-08-27 13:13:00

### 修复 (Fixed)
- **彻底根治本地打开出现无样式/裸奔 HTML 乱码界面问题**：
  - **根因诊断**：原项目在 `index.html` 中依赖外部网络 CDN `https://cdn.tailwindcss.com` 与 `importmap`。在国内网络或离线、网络波动时，外部 CDN 加载超时或受阻，导致 Tailwind CSS 样式规则无法注入，页面降级为原生 HTML 元素（即用户截图中的纯文本/白底黑字堆叠无样式界面，易被误解为 .jpg 破损图片）。同时本地开发时 Service Worker 缓存了无样式的 HTML。
  - **本地离线构建与脱耦**：安装配置本地 `tailwindcss`、`postcss`、`autoprefixer`，创建 `tailwind.config.js` 与 `postcss.config.js`，将 `@tailwind` 指令直接引入 `index.css`，由 Vite 本地打包编译生成完整 CSS 文件（约 61KB），彻底摆脱任何外部样式 CDN 依赖。
  - **开发环境 Service Worker 缓存隔离**：在 `index.html` 中增加环境判断，在 `localhost` / `127.0.0.1` 开发模式下自动注销旧版 Service Worker 并禁用缓存拦截，仅在 HTTPS 生产环境下开启 PWA 离线支持，确保本地开发 100% 毫秒级正常加载。

---

## [1.4.5] - 2026-08-27 12:28:00

### 新增与优化 (Added & Optimized)
- **TMDB 备选海报库海选与一键切换 (`tmdbService.ts` & `MovieForm.tsx`)**：
  - 在拉取 TMDB 详情时自动检索官方图库（`images.posters`），在表单海报区以横向滑轨展示备选海报缩略图，点击任意海报即可秒级下载压缩并替换封面，无需手动找图。
- **重温多刷时间流水与历史足迹 (`types.ts`, `MovieForm.tsx` & `MovieCard.tsx`)**：
  - 新增 `rewatchHistory` 数据结构，支持多刷时自动记录历次重温日期、评分与阶段性心得；在影视卡片详情中以精美微缩时间轴直观呈现每一刷的成长轨迹。
- **影评/短评一键快捷复制 (`MovieCard.tsx`)**：
  - 在卡片展开后的短评区增加一键复制按钮与轻量反馈，方便影迷快速转发分享自己的观影心得。
- **TMDB 电视剧智能分季选择与单季精细化录入 (`tmdbService.ts` & `TmdbSearchModal.tsx`)**：
  - **分季选择面板**：解决 TMDB 搜索默认将全剧（第 1 季到最新季）集数合并汇总且仅展示首季信息的痛点。选中电视剧时展示分季列表（含每季首播年份、单季集数、专属海报与单季简介）。
  - **单季/全剧灵活录入**：支持点选特定季度（如「怪奇物语 第4季」自动填充 2022 年、9 集与第 4 季专属海报），亦支持选择「全剧汇总」。
- **「新增记录」表单全能升级与录入体验深度重构**：
  - **表单分层与渐进式折叠 (`MovieForm.tsx`)**：默认聚焦高频核心字段（类型、标题、海报、状态、轮次、0.5星评分、日期、标签、笔记），将次要元数据（上映年份、国家地区、导演、时长、主演、观看平台、倍速折算）收纳进可折叠卡片，并实时展示已填充项数胶囊。
  - **0.5 半星精细评分体系 (`StarRating.tsx`)**：重构星级评分组件，支持左右半星分区交互与 50% 精准裁剪渲染；表单打分实时联动评级徽章（例如：`9.0分 · 神作` / `8.0分 · 推荐` / `6.0分 · 良作`）。
  - **剪贴板文本与豆瓣链接智能解析 (`clipboardParser.ts`)**：支持一键从剪贴板解析提取书名号片名、豆瓣/IMDb 链接、上映年份与剧集季度（`S01/第二季`），并自动联动 TMDB 搜索与本地匹配。
  - **Ctrl+V 截图直接粘贴海报 (`MovieForm.tsx`)**：支持全局及表单内剪贴板图片粘贴，自动压缩并写入海报封面，大幅提升电脑端录入效率。
  - **快捷日期胶囊 (`MovieForm.tsx`)**：提供「今天」「昨天」「前天」一键快速选定打卡日期，省去频繁调起系统原生日历选择器。
  - **草稿箱自动暂存与防误触恢复 (`useMovieForm.ts`)**：录入过程自动防抖暂存至 `sessionStorage`，再次打开添加弹窗时支持一键恢复未保存草稿与主动丢弃。

---

## [1.4.4] - 2026-08-27 00:22:00

### 优化 (Optimized)
- **移动端与触摸手势全方位深度适配**：
  - **剧集卡片大热区触控与触感反馈 (`MovieCard.tsx`)**：针对手机单手操作将 `+1 / -1` 快捷打卡按钮热区扩展至高响应度触控尺寸，引入 `touch-manipulation` 消除 300ms 点击延迟，增加 `active:scale-90` 触觉回弹动画。
  - **打卡热力图移动端自动居右与手势穿透 (`ActivityHeatmap.tsx`)**：52 周热力图在移动设备上挂载时自动平滑滚动至最右侧（当周活跃点），支持原生平滑惯性滑动 (`touch-pan-x` 与 iOS 动量滚动)；支持点击任意处或滑动时自动收起浮动气泡。
  - **海报生成与长图弹窗安全区域适配 (`ReportShareModal.tsx`)**：底部操作按钮全面接入 `env(safe-area-inset-bottom)` 避开 iOS 底部横条，高分辨率生成与一键复制按钮适配手机全宽手指触控。
  - **标签池与状态筛选滑轨触摸加速 (`App.tsx` & `MovieForm.tsx`)**：常用标签候选池、状态筛选栏增加触摸优化与平滑横滑手势支持。

---

## [1.4.3] - 2026-08-27 00:06:00

### 修复 (Fixed)
- **打卡时历史底账丢失导致统计倒扣减少问题彻底修复**：
  - **前序集数底账自动补齐 (`episodeUtils.ts`)**：修复当剧集之前未建立 `watchHistory`（或在表单中手动设置集数后）点击 `+1` 时，由于只追加了第 N+1 集单条流水，导致月度/年度按流水统计时前 N 集被全部丢弃、总集数与时长从 N 暴跌为 1 的严重 Bug。现自动将 1..N 集以 `addedAt` 作为底账基线安全补齐。
  - **编辑表单流水双向智能同步 (`MovieForm.tsx`)**：在弹窗修改电视剧当前集数时，自动同步扩充或裁剪 `watchHistory` 流水数组，彻底消除表单与卡片流水脱节隐患。
  - **全套统计计算公式严格复核与加固 (`statsCalculator.ts`)**：全面复核电影及电视剧的时长与集数计算公式；在所有兜底回退路径中严格纳入倍速（`playbackSpeed`）除法计算，确保倍速、时长、集数、多季、按月/按年多维统计 100% 严丝合缝。

---

## [1.4.2] - 2026-08-26 23:50:00

### 修复 (Fixed)
- **剧集集数统计与多季独立计算彻底修复**：
  - **保留多季剧集独立统计 (`titleNormalizer.ts`)**：修复原标题标准化算法强行剔除「第一季/第二季/Season 1/S01」导致不同季度被误判为同一部剧并互相覆盖集数的致命问题。现在多季剧集各自独立计算集数与时长，再无集数被吞噬现象。
  - **时间范围打卡集数精准统计 (`statsCalculator.ts`)**：在 `calculateTotalEpisodes` 与 `calculateTvDuration` 中新增 `TimeFilterOptions`，按月/按年筛选时精确统计落入所选周期的 `watchHistory` 打卡集数与实际时长，打卡后看板集数立即 +1 毫秒级跳变。

---

## [1.4.1] - 2026-08-26 23:32:00

### 修复 (Fixed)
- **追剧快捷打卡统计数据全链路实时联动修复**：
  - **时长实时动态计算**：修复 `calculateEpisodeUpdate` 遗漏同步计算 `actualWatchTime` 的问题。现在卡片一键 `+1 / -1` 打卡后，影视总时长（`tvDuration` 与 `totalDuration`）立即按当前倍速与单集时长毫秒级动态重算并更新看板。
  - **统计面板按月/按年过滤联动**：修复 `Stats.tsx` 仅按 `addedAt` 过滤导致历史剧集在当月打卡后未纳入当月统计的问题。现全面聚合 `watchHistory` 流水时间，当月打卡即刻计入当月时长与集数。
  - **观影趋势图动态联动**：修复趋势折线/柱状图（`trendData`）未计入追剧打卡足迹的问题，打卡操作实时推高当日活跃柱。
  - **主页时间范围筛选联动**：`App.tsx` 列表中「近 7 天 / 近 30 天 / 按年 / 按月」筛选项自动关联 `watchHistory` 打卡时间戳。

---

## [1.4.0] - 2026-08-26 23:17:00

### 新增 (Added)
- **海报 IndexedDB 大容量存储引擎 (`posterStorage.ts`)**：
  - 引入浏览器 IndexedDB 异步键值存储引擎，将本地用户上传的大体积 Base64 海报图片完全从 LocalStorage 分离，彻底解决 5MB 浏览器存储配额上限及数据溢出问题。
  - 支持海报自动后台备份、按 ID 秒级拉取、随记录删除联动销毁与孤立缓存清理。
  - 全流程安全防御：若环境不支持 IndexedDB，自动平滑无损降级，不阻断任何核心功能。
- **离线 PWA (Progressive Web App) 全链路支持**：
  - 新增标准 `public/sw.js` Service Worker 拦截与缓存引擎，配置 `cinelog-pwa-v1.4.0` 离线缓存。
  - 应用 Shell（HTML、CSS、JS、Manifest、图标）在离线状态下实现秒级极速打开。
  - API 与流式请求自动走网络直连，静态资源采用 Stale-While-Revalidate 高效响应，导航请求支持离线无缝回退。
  - 在 `index.html` 中安全注册 Service Worker，完美支持手机端与桌面端「添加到主屏幕 / 安装为独立 App」。

### 优化 (Optimized)
- **数据管理生命周期优化**：在 `useMovies` 中联动 IndexedDB，实现新增/修改/撤销/批量删除的海报数据生命周期严格对齐。
- **自动化测试全覆盖**：新增 `tests/posterStorageAndPwa.test.ts`，全量 9 套测试套件共 71 个用例 100% 验证通过。

---

## [1.3.0] - 2026-08-26 23:14:00

### 新增 (Added)
- **AI 影评实时流式输出 (SSE Stream)**：
  - 后端代理服务扩展 `/api/gemini/stream` Server-Sent Events (SSE) 接口，打通 Gemini `streamGenerateContent` 管道。
  - 前端服务与 `useMovieAi` 升级为打字机流式逐字累加渲染，点击「AI 帮我写」即刻秒级流式打字上屏，告别长时间空白等待。
  - 自动具备多级降级兜底保护：流式通道异常时自动无缝回退至全量 REST 请求。
- **年度 / 月度 / 全周期 观影报告高清长图生成 (`ReportShareModal`)**：
  - 全新打造影视手账海报生成器，支持一键渲染并导出 300% 超高清 PNG 长图及复制到剪贴板。
  - 支持多维度周期切换（全部全景、近一年、特定年度）。
  - 内置 4 款精美质感主题：**深空曜黑**、**赛博霓虹**、**极光幽林**、**蔚蓝星海**。
  - 深度聚合高阶观影指标：总观影时长（小时/分钟）、去重影视部数、累计追剧集数、年度高分精选海报卡片、偏好类型 TOP、常用自定义标签、重温之王与倍速高玩成就勋章。
  - 统计面板顶部直达「生成观影长图」入口。

### 优化 (Optimized)
- **统计工具库解耦与复用**：将 `calculateRewatchKing`、`calculateSpeedDemon`、`calculateJudgePersona` 抽取为全局公共纯函数，全面提升代码可测性。
- **全量单元测试覆盖**：新增 `tests/streamingAndReport.test.ts`，全量 8 套测试套件共 69 个用例 100% 通过。

---

## [1.2.0] - 2026-08-26 23:10:00

### 新增 (Added)
- **自定义标签 (Tags) 体系**：
  - 录入与编辑表单支持自定义标签输入（回车或逗号快速添加、Chip 标签一键移除）。
  - 表单智能汇集已有标签池，支持一键点击候选标签快速填充。
  - 卡片正面与折叠详情页完美展示标签 Chip 徽章。
  - 顶部筛选栏新增「所有标签」下拉筛选维度，支持按单标签精确过滤。
  - 全局模糊搜索自动关联匹配自定义标签，输入标签名即可秒级检索。
  - CSV / JSON 导入导出全链路支持自定义标签字段，无损迁移。
- **观影打卡热力图 (GitHub-Style Activity Heatmap)**：
  - 仿 GitHub 绿斑风格 52 周年度打卡日历热力图，完整展示每日电影观影与追剧打卡记录。
  - 支持「最近一年」及特定历史年份自由切换。
  - 实时统计并展示 4 大核心打卡指标：**累计打卡次数**、**活跃天数**、**当前连续天数**、**最长连续天数**。
  - 鼠标悬停/点击任意格子弹出精美毛玻璃气泡，显示当日观看的具体电影与电视剧集数详情。
  - 深度聚合电视剧 `watchHistory` 打卡流水与电影添加足迹，真实还原观影生活轨迹。

### 优化 (Optimized)
- **统计面板整合升级**：在统计面板中将打卡热力图与核心数据指标、类型饼图、观影雷达图深度结合。
- **全量单元测试覆盖**：新增 `tests/tagsAndHeatmap.test.ts`，全量 66 个测试用例 100% 通过。

---

## [1.1.0] - 2026-08-26 23:03:00

### 新增 (Added)
- **追剧卡片一键 `+1 / -1` 快捷打卡**：在电视剧（TV）卡片正面直接提供极速打卡胶囊按钮，无需点开编辑表单即可秒级推进追剧集数。
- **自动记录每集观看时间流水 (`watchHistory`)**：点击 `+1` 打卡时，系统自动在 `watchHistory` 追加当前集数与毫秒级时间戳，并在卡片上友好展示「最近打卡：刚刚 / 今天 22:58 / 8月26日」。
- **全剧完结智能流转**：当集数打卡递增至最后一集（如 12/12）时，系统自动流转状态为「完结 (WATCHED)」并弹出完结祝贺 Toast。
- **追剧打卡足迹时间轴**：在卡片折叠详情区中新增「追剧打卡足迹」列表，完整呈现历史每集的打卡时间流水。
- **全局效率快捷键支持**：
  - `Ctrl / Cmd + K`：一键聚焦顶部搜索框并全选搜索词。
  - `N`：在主界面快速呼出「新建观影记录」表单。
  - `Esc`：快速关闭当前弹窗（新建/编辑弹窗、云同步弹窗、批量管理模式等）。
  - 界面输入框与按钮中增加对应的键位提示胶囊 Badge。

### 优化 (Optimized)
- **导出模块静态化优化**：修复 CSV 导出多重动态 import 的冗余告警，提升构建与打包速度。
- **单元测试覆盖**：新增 `quickEpisodeUpdate.test.ts` 全套针对打卡逻辑与时间格式化的自动化测试（共 62 个测试全量 PASS）。

---

## [1.0.5] - 2026-06-22 11:25:00

### 修复 (Fixed)
- **GitHub 接口挂起锁死修复**：为所有 GitHub Gist 相关的 fetch 请求添加了 8 秒超时机制。防止因国内 GitHub 网络问题导致 fetch 持续 Pending，从而锁定 `isSyncing` 状态，使“上传到云端”和“从云端下载”按钮永久灰显不可用。
- **Token 验证防网络拥堵优化**：重构了 `verifyToken` 接口，能够有效区分“网络错误”与“无效 Token”。当网络不通时，允许用户强制保存 Token，解决了用户在断网或网络不好时无法通过验证导致无法保存 Token 并将按钮解锁的问题。

## [1.0.4] - 2026-06-21 11:55:00

### 新增 (Added)
- **自动云同步支持**：在“云端同步”面板中新增了**「自动云同步」**Toggle 开关。开启后，应用无需用户手动上传或下载，全程后台自动处理。
- **页面启动自动同步**：每次启动/打开应用时，如果已开启自动同步且配置了 Token，将在 1 秒后静默拉取云端数据并执行双向合并。
- **数据变动防抖自动同步**：在用户进行新增、修改、删除或批量删除等操作后，自动触发防抖（5 秒延迟），静默将最新状态上传至 GitHub Gist。
- **每日定时静默同步**：当应用保持开启时，每隔 1 小时检测一次。若距离上次同步超过 24 小时，自动在后台静默执行一次完整同步对齐。
- **智能双向合并算法**：重构了数据合并机制，基于 `lastUpdated` 修改时间戳对比本地与云端每条记录，自动保留最新修改，多端数据智能合并。
- **删除冲突解决（Tombstone 机制）**：引入本地删除库（`cinelog_deleted_movies_v1`），记录被删除记录的 ID 和删除时间，确保一端执行删除后，在多端同步中能自动同步删除，不会被旧数据误恢复；且若某条记录在被删后于云端又被更新，则会自动移除删除标记并恢复该记录。
