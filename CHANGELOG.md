# 更新日志 (Changelog)

所有针对“CineLog 观影记录”项目的更改都会记录在此文件中。
本日志遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 规范。

---

## [1.0.4] - 2026-06-21

### 新增 (Added)
- **自动云同步支持**：在“云端同步”面板中新增了**「自动云同步」**Toggle 开关。开启后，应用无需用户手动上传或下载，全程后台自动处理。
- **页面启动自动同步**：每次启动/打开应用时，如果已开启自动同步且配置了 Token，将在 1 秒后静默拉取云端数据并执行双向合并。
- **数据变动防抖自动同步**：在用户进行新增、修改、删除或批量删除等操作后，自动触发防抖（5 秒延迟），静默将最新状态上传至 GitHub Gist。
- **每日定时静默同步**：当应用保持开启时，每隔 1 小时检测一次。若距离上次同步超过 24 小时，自动在后台静默执行一次完整同步对齐。
- **智能双向合并算法**：重构了数据合并机制，基于 `lastUpdated` 修改时间戳对比本地与云端每条记录，自动保留最新修改，多端数据智能合并。
- **删除冲突解决（Tombstone 机制）**：引入本地删除库（`cinelog_deleted_movies_v1`），记录被删除记录的 ID 和删除时间，确保一端执行删除后，在多端同步中能自动同步删除，不会被旧数据误恢复；且若某条记录在被删后于云端又被更新，则会自动移除删除标记并恢复该记录。

## [1.0.3] - 2026-06-18

### 修复 (Fixed)
- **剧集状态默认逻辑**：电视剧首次记录时，默认状态自动设定为“追剧中”，且在切换媒介类型（电影与电视剧）时能够自适应调整初始状态。
- **重温已看集数重置**：修复开启电视剧多次观看（多刷/重温）时，已看集数累计超出总集数的问题。重温开始时已看集数将归零重计，且单次输入已看集数不会超出总集数。
- **TMDB 实际时长适配**：电视剧单集时长自动读取并适配 TMDB 接口返回的实际单集时长（如 `episode_run_time`、`runtime` 等），移除了固定的 45 分钟默认硬编码。
- **重温足迹刷数展示**：修复卡片详情页中，未完结前显示“重温足迹（N刷）”的逻辑，确保未完结时仅显示为当前首刷（1刷），完结后才正确统计并显示历史多刷重温轨迹。

## [1.0.2] - 2026-06-13

### 新增 (Added)
- **多刷筛选**：在主界面顶部的状态过滤栏（“追剧中”后面）新增了**「多刷」**筛选条件，可一键过滤并只显示重温（二刷及以上）的影片与剧集。

### 优化 (Changed)
- **多刷次数自动推荐**：将表单中自动推荐多刷次数的计算逻辑，从单一依靠已完结历史数量升级为 `Math.max(已完结记录数, 历史记录中最大刷数) + 1`，更智能地兼容手动设定的高刷数记录。
- **自动化测试加固**：新增了包含真实云端数据下载、合并与过滤渲染的集成测试用例，提高了 React 组件渲染的健壮性。

### 修复 (Fixed)
- **云端下载黑屏白屏**：修复了 `Stats.tsx` 面板的 `useMemo` 返回值中漏解构 `rewatchRate` 与 `rewatchKing` 导致的致命 `ReferenceError` 错误。消除了用户点击“从云端下载”更新本地数据后导致页面渲染崩溃的问题。

## [1.0.1] - 2026-02-28

### 优化 (Changed)
- **iOS 桌面图标与 PWA 体验**：优化了 `manifest.json` 与 `index.html` 的 iOS 适配标签。图标现在支持 `maskable` 属性并设置了深色背景填充（#0F172A），解决了添加到主屏幕后可能出现的白边问题。
- **统计面板去重机制**：电视剧的数据聚合策略大幅优化。“类型分布”、“状态分布”及“评分分布”现在严格按“单部剧”计算，杜绝了由于某部剧集数过多（如几百集）导致的数据严重倾斜现象。- **环境稳定性**：移除了对系统全局 `npx` 的依赖，减少跨设备开发或终端环境异常时的崩溃风险。

### 修复 (Fixed)
- **429 配额限制优化**：针对 Gemini 免费版 API 每分钟请求频率限制（Rate Limit），在服务端引入了自动重试机制与指数退避算法，并增加了友好的中文错误提示。
- **全局网络穿透修复**：通过 `undici` 配合本机代理自动接管服务端请求，彻底解决了由于地理位置限制导致的 `User location is not supported` 错误。
- **AI 代理通信故障**：修复了在触发“AI 帮我写”或“AI 观影管家”时抛出 `Failed to fetch` 的错误，现已让本地 Node.js 代理服务基于本地依赖与 Vite 前端无缝并且稳定地同时启动。
---

## [1.0.0] - 2026-02-27之前

### 初始架构 (Added)
- **核心功能**：支持记录电影与电视剧的观影进度、打分与评价。
- **高级统计**：内置基于时间和类型的多维观影图表 (Recharts)。
- **AI 赋能**：集成 Google Gemini大模型进行 AI 海报色提取、AI 自动影评、观影风格洞察与智能推荐。
- **云端存储**：实现了基于 Github Gist 的纯前端、零后端服务器的云同步机制。
- **PWA 及响应式优化**：针对移动端屏幕交互和 iOS Safari “添加到主屏幕” 做了深度体验适配。
